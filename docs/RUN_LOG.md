# Run Log — Gaslight & Grimoire

> Append-only history of working sessions. Newest entry at the **top**. One entry per session.
> Keep entries short — detail that's still *current* belongs in [PROJECT_STATE.md](PROJECT_STATE.md);
> decisions belong in [DECISIONS/](DECISIONS/). This log is the *narrative of what happened, when*.

---

## Entry template (copy this)

```
## YYYY-MM-DD — <short title>
- **Goal:** <what this session set out to do>
- **Did:** <what actually changed — files, decisions, builds>
- **Verified:** <commands/tests run + their results, or "n/a — no code yet">
- **Open / blockers:** <what's unresolved>
- **Memory updated:** STATE ☐ · RUN_LOG ☑ · ADR ☐ (<id if any>)
```

---

## 2026-07-07 — Clear final P0 group: encounter escape (#4) + onEnter idempotency (#5), +F-022/F-027

- **Goal:** Finish the audit's P0 backlog — the two remaining engine bugs (#4, #5) — and fold in any adjacent findings that live on the same engine surface and make sense to fix in the same pass.
- **Did:** One branch/PR (**PR #28**, merged `2efb40a`), TDD throughout (RED watched before every fix). **#4 (F-004)** — `processEncounterChoice` now treats `isEscapePath` choices as terminal: navigates to the escape outcome and marks the encounter `isComplete` immediately, before the round-advance logic, applying no damage. **#5 (F-006/F-008)** — moved `onEnter` effect application out of the `NarrativePanel` useEffect into `goToScene`, gated on a new `narrativeSlice.visitedScenes` set so a scene's effects fire exactly once per playthrough; feedback text now flows through `narrativeSlice.lastEffectMessages` (panel only reads it). `visitedScenes` added to `GameState`/snapshot/`loadGame`; save migration **v2→v3** backfills it from `sceneHistory + currentScene`, and `CURRENT_SAVE_VERSION` bumped 2→3. Reset on case/vignette load. **Graft-ins on the same surface:** **F-022** — `computeChoiceResult` throws a clear error for a non-check choice with no `success`/`critical` outcome (was silent `goToScene(undefined)` → blank scene), plus a matching content-validator rule (partial F-016). **F-027** — removed the dead `_hasAdvantage` annotation from `getEncounterChoices` (no production consumer; `getEncounterChoices` now returns `Choice[]`); rewrote its 3 tests to assert the real advantage passed to `performCheck` inside `processEncounterChoice`. **Left out** (own PRs): #10 slug titles, F-013 typed `lastCriticalFaculty`, #9 breakdown dead-end.
- **Correction to the audit framing:** F-006's re-fire was attributed partly to the back button, but that button uses a read-only review overlay (`App.tsx` `setReviewSceneId`), not `goToScene` — so it never re-fired. The real trigger was **save-load** landing on an already-entered scene; the visited-set fix covers it. Also confirmed every recovery scene with a positive `onEnter` delta is a one-time story beat (no scene is designed to re-heal on re-entry), so once-per-playthrough is the correct semantics.
- **Verified:** `npm run test:run` → **384 passed (384)**, 30 files (was 368; +16 new tests, 0 regressions). `node scripts/validateCase.mjs` → 7 cases clean (new non-check-outcome rule flags nothing in existing content). `tsc --noEmit` + `npm run build` green. CI green on PR #28: test ✅ → build ✅, deploy skipped on PR, OWASP + npm audit ✅. Issues #4/#5 auto-closed → **0 P0 issues open**.
- **Doc-drift sweep (auto-fixed this session's drift):** `engine-reference.md` — rewrote `computeChoiceResult` (throws on no-outcome), `processEncounterChoice` (escape terminal), `getEncounterChoices` (now `Choice[]`, `_hasAdvantage` gone), `SaveManager.migrate` (added v2→v3, current version 3). `architecture.md` — data-flow diagram (onEnter now from `goToScene`, gated), narrativeSlice state row (+`visitedScenes`/`lastEffectMessages`). `CLAUDE.md` — narrativeSlice row, `applyEffects`/`processChoice` behaviour lines, encounter behaviour lines, Save System migration line (also fixed the **pre-existing** "Current version: 1" drift flagged as #17, since this session touched the migration), test baseline 368→384, and the `applyEffects`-from-view architectural warning (now onEnter-from-`goToScene`). `status.md` + `PROJECT_STATE.md` test baseline 368→384. No broken internal links introduced.
- **Open / blockers:** P0 backlog fully cleared. Next work queue is **P1** (issues #6–#12), starting with #6 (deduction-gated content). No blockers. Note: `CLAUDE.md:51` `<GameScreen>` and the high-contrast-inert items from #17/#8 remain open in their own issues (not touched here).
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new architectural decision — fixes within existing patterns; the `visitedScenes` gate implements the content↔engine/idempotency direction already established, ADR-0001).

## 2026-07-07 — Content-authoring automation layer + CLAUDE.md currency + Debt of Smoke design fixes

- **Goal:** Audit/improve `CLAUDE.md`, then design and wire in a set of Claude Code automations for the content-authoring workflow, and apply the design fixes the new reviewer surfaced.
- **Did:** **(1) CLAUDE.md currency** — corrected the stale CI/CD section (now describes the `test`→`build`→`deploy` job graph, `pull_request` trigger, PR-deploy skip), dropped the drifting "as of 2026-02-25" Known-Bugs date and pointed it at `status.md`/`PROJECT_STATE.md`, and added the two missing files (`engineActions.ts`, `scripts/validateCase.ts`) to the Directory Layout. **(2) Automation layer (ADR-0004)** — `PostToolUse` hook `.claude/hooks/post-edit-checks.sh` (validator on `public/content/**.json` edits + `tsc --noEmit` on `.ts`/`.tsx` edits, surfaces failures via exit 2, `TYPECHECK=0` escape hatch) wired into `.claude/settings.json`; read-only `content-integrity-reviewer` subagent (`.claude/agents/`) for the design/tone rules the validator can't check; new `/review-content` command; `/new-scene` scaffold skill + annotated `scene-template.json`. Wired the reviewer into `/new-scene` (final step) and `/checkpoint` (content gate, self-skips when no `public/content/` changed) since hooks can't dispatch subagents. Added `context7` + `github` MCP servers to `.mcp.json` (project scope; `gh auth login` done). **(3) Debt of Smoke fixes** — the reviewer's first run flagged 4 findings; fixed the 2 warnings: `dos-choice-who-threat` had an inert success band (success/partial routed to the same guarded scene as fumble) → now matches its influence sibling; the guarded branch routed straight to Act 2, skipping the only source of the central `dos-clue-ember-artifact` clue and Sable's payoff → rerouted through `dos-act1-sable-reveals`, with the guarded scene's closing line reworded so the relent reads coherently. Left the 2 nits (toothless red herring, twin Ashwick faculty paths) as optional.
- **Verified:** `content-integrity-reviewer` dispatched twice against Debt of Smoke — first run 4 findings, post-fix re-review **0 blockers / 0 warnings**, no regressions/orphans. `node scripts/validateCase.mjs` → 7 cases clean. `npm run test:run` → **368 passed (368)**, 30 files. `tsc --noEmit` clean (`src` scope). Hook exercised on 5 payloads: passing content JSON / passing TS / non-matching file all exit 0 quiet; **broken content edge → exit 2 with the validator error surfaced; broken TS → exit 2** — test fixtures restored.
- **Doc-drift sweep (auto-fixed this session's drift):** `PROJECT_STATE.md` said "up to date with origin at `dd18933`" and "post-merge deploy on `dd18933` running" — stale (HEAD is now `4036be6`, PR #26 merged, that deploy long done); corrected the branch-focus + verification lines to `4036be6`. `status.md` test baseline (368/30) and content counts still accurate — no change. No broken internal links introduced.
- **Open / blockers:** P0 engine bugs **#4** + **#5** still the next feature work (unchanged). Optional: 2 nit-level Debt of Smoke polish items; the `PostToolUse` type-check may prove noisy on multi-file refactors (mitigated by `TYPECHECK=0`). No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☑ (ADR-0004 — content-authoring automation layer)

## 2026-07-07 — Audit remediation: P0 group 1 (CI gate) + group 2 (validators + Debt of Smoke)

- **Goal:** Start clearing the audit's P0 backlog. Grouped the issues and knocked out two clusters: the CI safety net (#1 + #11) and the content-validator unification + the unreachable vignette (#2 + #3).
- **Did:** **PR #24 (merged `39a57f6`)** — added a `test` job to `deploy.yml` running the validator + `npm run test:run`; `build` → `deploy` now depend on it; added a `pull_request` trigger with `deploy` skipped on PR events (closes **#1**). Deploy quick-wins (closes **#11**): themed `public/favicon.svg` referenced via `%BASE_URL%` (was a `/vite.svg` 404); `cancel-in-progress: false`; pinned `dependency-check/Dependency-Check_Action` to SHA `75ba02d` (v1.1.0). **PR #25 (merged `dd18933`)** — new pure `src/engine/contentValidation.ts` (`validateBundle`) is now the single source of truth for both the runtime `validateContent` (delegates, errors only) and the CLI (`scripts/validateCase.ts` via `vite-node`, with `validateCase.mjs` kept as a launcher shim); extended checks to condition targets, variant structure, `npcEffect` refs, encounter-round recursion, `dynamicDifficulty` tier completeness, `clue.sceneSource`, + CLI reachability (closes **#2**). Fixed **F-024** engine bug (`evaluateCondition` `hasFlag` `{value:false}` never matched an unset flag → 6 recovery variants dead; now `Boolean(flag)===value`) and **F-023** (4 dangling Mayfair `sceneSource`s repointed). Re-gated *Debt of Smoke* from the unattainable `npc-sable` disposition ≥ 7 onto the persisted `wc-court-deal-made` flag (closes **#3**). Promoted `vite-node` to a declared devDependency. Built with TDD (RED watched before each fix).
- **Verified:** `npm run test:run` → **368 passed (368)**, 30 files (was 334/29). `node scripts/validateCase.mjs` → 7 cases clean; a deliberately-broken scratch case errors on every new check class and exits 1. `npm run build` + `tsc --noEmit` green. CI green on both PRs in the clean runner (test ✅ / build ✅ / deploy skipped on PR); OWASP scan ✅ with the pinned SHA. Issues #1/#11/#2/#3 auto-closed; 18 open remain.
- **Doc-drift sweep (auto-fixed this session's drift):** test baseline **334→368 / 29→30 files** in `docs/status.md`, `CLAUDE.md`, `PROJECT_STATE.md`. *Debt of Smoke* unlock (disposition ≥ 7 → `wc-court-deal-made` flag) in `CLAUDE.md` (×2), `docs/engine-reference.md`. Validator scope/CI-step description rewritten in `docs/content-authoring.md` + `docs/status.md` (now the shared module, wider checks, `test`-job step). Added `contentValidation.ts` to the module lists in `CLAUDE.md` + `docs/engine-reference.md`; corrected the `hasFlag`/`validateContent`/`checkVignetteUnlocks` descriptions there. RUN_LOG/audit records left as point-in-time (not rewritten).
- **Open / blockers:** P0 group remaining: engine bugs **#4** (encounter escape is a dead button before the final round) + **#5** (`onEnter` re-fires on revisit/load) — next session. No blockers. Post-merge deploy on `dd18933` running.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new architectural decision — fixes within existing patterns; shared-validator extraction is an implementation of the existing content↔engine separation, ADR-0001)

## 2026-07-07 — Full Ultracode repo audit + file findings as GitHub issues

- **Goal:** Run a comprehensive read-only, all-angles audit of the repo and produce a report for later remediation; then file the findings as GitHub issues.
- **Did:** Ran a 13-dimension adversarial audit (110 subagents: one finder per dimension, one independent skeptic re-opening cited files per finding). 97 findings verified, 5 rejected as false positives; deduped to **67 canonical findings** (2 High, ~19 Medium, ~40 Low/Polish). Wrote the report (now at `docs/audits/ULTRACODE_FULL_REPO_ANALYSIS.md` — the user moved it there from repo root). Created priority + category labels and filed **22 grouped GitHub issues** (#1–#22: 5 P0, 7 P1, 7 P2, 3 P3) on `JonWhiteFang/gaslight-and-grimoire`. Independently re-verified the anchor findings (e.g. `npc-sable` max disposition +4 vs unlock threshold 7 → *Debt of Smoke* unreachable; 0 `requiresDeduction`/`hasDeduction` refs in content; `ClueCard` has `draggable` + `onKeyDown` only, no click/touch). Updated PROJECT_STATE to correct the "media is the only remaining work" framing (now false), add an audit + a P0/P1 remediation milestone row, reorder Next actions, and link the report. **No code changed.**
- **Verified:** `npm run test:run` → **334 passed (334)**, 29 files (unchanged). `node scripts/validateCase.mjs` → 7 cases clean. `npm run build` green (402 KB JS / 125 KB gzip). `npm audit` → 0 vulns. `gh issue list` → 22 open issues. Audit method is read-only; runtime-behaviour findings (encounter escape, onEnter re-fire, touch connect) are confirmed from code+content and flagged in the report for an optional Playwright MCP click-through.
- **Doc-drift sweep (flagged, NOT auto-fixed — each is now a filed issue, so fixing piecemeal here would make the issue stale on day one):** (1) `CLAUDE.md:197` says save "Current version: 1" but `saveManager.ts:14` is `CURRENT_SAVE_VERSION = 2`; `CLAUDE.md:305` wrongly lists `saveManager` under `Date.now()/Math.random()` users → **issue #17**. (2) `docs/status.md:70`/`CLAUDE.md` present high-contrast as "applied", but the audit found the `.high-contrast` class is consumed by nothing (inert) → **issue #8**. (3) `CLAUDE.md:51` references a `<GameScreen>` component that doesn't exist → **issue #17**. Left these to their issues rather than editing four bundled drifts one at a time. No drift found in the spine itself.
- **Open / blockers:** 22-issue backlog is the new work queue; P0 issues (#1–#5) are correctness/CI blockers that should precede media assets. No blockers. The relocated report under `docs/audits/` is untracked — surfaced for the user to commit.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new architectural decision — the audit records findings, it doesn't decide a direction)

## 2026-07-07 — Add Playwright MCP server (project scope)

- **Goal:** Install a browser-driving MCP capability so the running game can be verified end-to-end (the one open milestone, media assets, only shows up at runtime).
- **Did:** Added `@playwright/mcp` at **project scope** via committed `.mcp.json` (`claude mcp add playwright -s project`). Chose project over local/user scope for team-shareability, consistent with the repo's committed-tooling convention. Recorded the rationale in **ADR-0003** (Enacted) and wired it into the ADR index. Updated PROJECT_STATE: `_Last updated:_`, References (ADR-0003), and Next actions (media-asset steps now note Playwright can verify in-browser).
- **Verified:** `.mcp.json` valid JSON, registers `playwright`; `npx @playwright/mcp@latest --version` → 0.0.77; Playwright Chromium binary already cached. `claude mcp list` → server registered, `⏸ Pending approval` (expected one-time gate for committed MCP). Ground-truth baseline unchanged: `npm run test:run` → **334 passed (334)**, 29 files; `node scripts/validateCase.mjs` → 7 cases clean. Doc-drift sweep: no doc references MCP/Playwright, so no drift; STATE counts still accurate.
- **Open / blockers:** Playwright MCP needs one-time approval on next `claude` launch in this repo before its tools go live. Media assets remain the one open milestone. No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☑ (ADR-0003)

## 2026-07-07 — First `/checkpoint`: exercise the loop, enact ADR-0002

- **Goal:** Run the first real end-of-session `/checkpoint` to verify the memory-spine loop works, and enact ADR-0002 now that its follow-up condition is met.
- **Did:** Doc-drift sweep (scoped to this session's docs) found no drift — STATE/status.md `334/334` + `29 files`, content counts, and the CLAUDE.md hook path all match code/git/config. Flipped **ADR-0002 Accepted→Enacted** (status line annotated with the transition; Follow-up struck through with a dated note — original decision text preserved). Updated the ADR index row to Enacted. Bumped PROJECT_STATE `_Last updated:_` and marked both ADRs Enacted in References. Confirmed the `/checkpoint` skill and `SessionStart` hook load (post `/reload-plugins`: "2 hooks, 1 skill").
- **Verified:** `npm run test:run` → **334 passed (334)**, 29 files. `node scripts/validateCase.mjs` → all 7 cases clean. Working tree was clean at session start (prior spine work already committed as `04e7485` and pushed).
- **Open / blockers:** Media assets remain the one open milestone — see PROJECT_STATE Open questions. No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☑ (ADR-0002 → Enacted)

## 2026-07-07 — Stand up the committed memory spine

- **Goal:** Set up the committed, version-controlled project-memory spine (PROJECT_STATE / RUN_LOG / DECISIONS + session-start hook + `/checkpoint` skill), adapted to G&G's real state.
- **Did:** Added `docs/PROJECT_STATE.md` (one-page snapshot; phase tracker A–E done, media-assets the one open milestone; scope-gate tracker and scope ledger dropped as not applicable to a feature-complete project). Added `docs/RUN_LOG.md` (this file). Added `docs/DECISIONS/` with `ADR-TEMPLATE.md`, `README.md` (index), `ADR-0001-content-engine-separation.md` (architecture baseline, Enacted), and `ADR-0002-committed-memory-spine.md` (this system, Accepted). Added `.claude/hooks/session-preflight.sh` (git state + top of PROJECT_STATE) and wired a `SessionStart` hook into `.claude/settings.json` while preserving the existing `permissions` block. Added `.claude/skills/checkpoint/SKILL.md`. Added a "Project memory" pointer section to `CLAUDE.md`.
- **Verified:** `npm run test:run` → **334 passed (334)**, 29 files. `node scripts/validateCase.mjs` → all 7 cases clean. `.claude/settings.json` parses as valid JSON (`jq`). Hook script runs standalone and emits valid `additionalContext` JSON.
- **Open / blockers:** Media assets (audio + illustrations + NPC portraits) remain unbuilt — see PROJECT_STATE Open questions. No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☑ (ADR-0001, ADR-0002)
