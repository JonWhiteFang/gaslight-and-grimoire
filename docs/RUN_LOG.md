# Run Log — Gaslight & Grimoire

> Prepend-only history of working sessions. New entries at the **top**; never edit past entries. One entry per session.
> Keep entries short — detail that's still *current* belongs in [PROJECT_STATE.md](PROJECT_STATE.md);
> decisions belong in [DECISIONS/](DECISIONS/). This log is the *narrative of what happened, when*.

---

## Entry template (copy this)

```
## YYYY-MM-DD — <short title>
- **Goal:** <what this session set out to do>
- **Did:** <what actually changed — files, decisions, builds>
- **Verified:** <commands/tests run + their results, or "n/a — no code yet">
- **Open / blockers:** <what's unresolved>
- **Memory updated:** STATE ☐ · RUN_LOG ☑ · ADR ☐ (<id if any>)
```

---

## 2026-07-18 — The Orrery Room MERGED (PR #87)

- **Goal:** Merge the completed Orrery Room branch once CI passed; sync `main`; flip the spine to "merged".
- **Did:** All 6 CI checks green on PR #87 → merged via `gh pr merge 87 --merge --delete-branch` (merge commit `1aba478`, never squashed); local `main` synced (`git pull --ff-only`), remote branch pruned. Spine updated: PROJECT_STATE header (merge line prepended), Branch-focus → none/`main`, C2 tracker row → MERGED, Next-actions → The Drowned Archive. No code touched post-merge.
- **Verified:** CI on PR #87: test, build, npm audit, OWASP, dependency-check, Cloudflare Workers Build — all pass. `main` at `1aba478` carries the full 16-commit branch history (no squash).
- **Open / blockers:** None. Catalog: 9 units (4 main + 5 vignettes); Mythos keystone pair live. Next: The Drowned Archive → The Tidewaiter's Log. Unchanged: #20 (media, user-blocked).
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (merge + sync only).

## 2026-07-18 — The Orrery Room BUILT — Grey Dawn vignette + Mythos keystone (PR #87)

- **Goal:** Build The Orrery Room end-to-end — the fifth side vignette (Grey Dawn faction gap + the Mythos thread's keystone deduction), including the engine work to make vignette recipes/variants first-class.
- **Did:** Brainstorm (4 forks: flagship scale ~21 scenes; extend vignettes with recipes+variants; full-mystery-either-way with the keystone as a Phase 5 disabled-with-reason breadcrumb; 3 endings by faction verdict + keystone variants) → spec → **Codex spec pass (10 findings — 2 Blockers: unlock registry is a hard-coded engine list, CLI never loads vignette variants; plus onForm-at-formation, veil-variant mechanism, discoverable-clues onEnter gap, F-102 flag-blindness, clamp-ordering)** → plan (11 tasks) → **Codex plan pass (9 findings — 2 Blockers: missing `fumble` tier on every check, T2 fixture 404'd the mandatory shared scenes; plus T3 false-green RED fixture, single zero/zero content commit, ESM/helper corrections, 5 behavioral integration witnesses, 21-scene count)** → **subagent-driven execution**: T1 unlock registry, T2 loader/adapter optional recipes+variants, T3 CLI vignette variants + broken-variant fixture, T4 `computeDiscoverableClues` onEnter parity, T5 `KeyDeduction.onForm` (once-guard), T6–T8 content as ONE zero/zero commit (21 base + 4 variants / 7 clues / 2 recipes / 3 NPCs), T9 witness+integration tests (mutation-verified) incl. the form-keystone-after-terminal witness, T10 docs. **Content-integrity review** (1 Major: laudanum↔diary connectsTo edge missing — the correct mundane inference classified `incorrect` on the board; + gate-neutral keystone label, partial convention, Finch retry cost, onward gate) folded. **Whole-branch integration review** (1 Major: the Case Complete screen read the BASE scene map — the repo's first terminal-scene variants made it live; fixed via exported `resolveEndingNarrative`; + the onEnter once-per-playthrough caveat documented) folded. **Live Playwright verify** (locked/unlocked keystone, board formation → deduction + persistent flag, `-named` ending + variant-quoting completion screen, 0 console errors). **Codex impl pass (2 Major + 2 Minor, all folded):** status-aware optional loading (404-only fallback — a 500/malformed variants.json would have silently stripped the earned endings), effects-before-publish onForm + unknown-effect-type validation (a throwing onForm stranded a half-formed deduction that never retried), App-level completion seam test (the helper test alone was false-green at the seam), doc drift. Pushed, opened **PR #87** (merge commit, never squash).
- **Verified:** on `e7e1b86`: `npm run test:run` → **858 passed (858)** / **89** files (was 816/82); `npm run lint` clean; `node scripts/validateCase.mjs` → **9 units, zero errors, zero warnings**; `npm run build` green. Mutation checks watched RED: keystone visibility flip, ending effect-order swap, completion-seam reversion.
- **Open / blockers:** **Merge PR [#87](https://github.com/JonWhiteFang/gaslight-and-grimoire/pull/87).** Then content continues: The Drowned Archive → The Tidewaiter's Log. Unchanged: #20 (media, user-blocked); Comet Club bookkeeping flags; `getEncounterChoices` partition future touch.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no standalone decision — the four design forks live in the Codex-reviewed spec; `onForm` and the loading-policy hardening are review folds within the spec's model). **Sweep:** `status.md` baseline → 858/89 (done in the impl fold); PROJECT_STATE header/Branch-focus/Verification/tracker (new C2 row)/Next-actions refreshed; engine-reference generic-path wording + `CaseData.recipes` comment de-drifted (Codex impl Minor 4).

## 2026-07-17 — Phase 5 MERGED (PR #86) — UI/UX roadmap complete

- **Goal:** Merge the completed Phase 5 branch once CI passed; sync `main`; flip the spine from "PR open" to "merged".
- **Did:** All 6 CI checks green on PR #86 → merged via `gh pr merge 86 --merge --delete-branch` (merge commit `a1ddbd0`, never squashed); synced local `main` (`git pull --ff-only`), pruned the deleted remote branch. Spine updated: PROJECT_STATE header (merge line prepended), Branch-focus → none/`main`, U5 tracker row → MERGED, Next-actions → UI/UX track fully complete, content resumes (The Orrery Room). No code touched post-merge.
- **Verified:** CI on PR #86: test, build, npm audit, OWASP, dependency-check, Cloudflare Workers Build — all pass. `main` at `a1ddbd0` carries the full 24-commit Phase 5 history (no squash).
- **Open / blockers:** None. **The UI/UX roadmap (Phases 0–5) is fully complete.** Next: The Orrery Room (Grey Dawn vignette). Unchanged: #20 (media, user-blocked).
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (merge + sync only).

## 2026-07-17 — Phase 5 IMPLEMENTED — choice-gating content model shipped (PR #86, final UI/UX roadmap item)

- **Goal:** Build the last UI/UX roadmap item: a `visibility`/`gateReason` vocabulary so authors can show a gated choice disabled-with-a-diegetic-reason instead of hard-hidden — schema, engine resolver, validator rules, rendering, one demo conversion, docs; backward-compatible with all 8 shipped cases.
- **Did:** Brainstorm (4 forks resolved: visibility governs the unmet-gate case, default `hidden`; author-written validator-enforced `gateReason`; vocabulary + one demo, no retrofit; greyed non-interactive always-visible reason) → spec → **Codex spec pass** (5 Major + 2 Minor, all folded — escape-path exclusion, gateReason iff-disabled both directions, the `choiceGateConditions().length>0` gate predicate + `requiresFlag:''` compat, `warnings` into validator `Ctx`, demo-as-sole-delta) → plan (9 TDD tasks) → **Codex plan pass** (1 Blocker + 4 Major + 1 Minor, all folded — wrong `ContentBundle` import (TS2305), LockedChoice opacity broke AA contrast, false-green warning test, missing component-test roles/order/focus, demo witness test). Executed via **subagent-driven-development** (fresh implementer + spec + quality reviewer per task; all review folds separate commits): **T1** `Choice.visibility`+`gateReason`; **T2** pure `choiceVisibility.ts` (`choiceGateConditions` + `resolveChoiceVisibility`, barrel-exported); **T3** validator — 5 errors + soft-gate warning, `warnings` threaded into `Ctx`, `VISIBILITY_VALUES` ReadonlySet fold; **T4** shared `LockedChoice` (non-interactive `<li>`, 🔒+line-through+reason, no opacity); **T5** ChoicePanel partition → nav + `<ul role="list">` locked group, `isChoiceVisible` deleted (12 pure-function tests migrated to the resolver), role=list/rename/empty-reason folds; **T6** encounters via shared resolver (escape paths byte-identical hard-gate) + EncounterPanel locked group + hidden-exclusion/escape-disabled coverage folds; **T7** demo — Comet Club `cc-act2-hub`/`cc-choice-hub-halloway` (`requiresFlag: cc-halloway-trusts`) + content-backed before/after witness test; content-integrity review folded (gate-neutral label "Present yourself at Lady Halloway's drawing room."; softened reason); **T8** docs (content-authoring hide-vs-disable rules + gateReason tone + gate-neutral-label rule; engine-reference; architecture) + fidelity-review fold (escape-path over-claim). **T9:** full gate → **whole-branch integration review** (Ready to merge; 4 Minors, 3 folded — all-gated-round validator warning, defense-in-depth select-handler visibility guards (stash-verified RED), `worseAlternative`×`visibility` authoring note) → **live Playwright verify** (locked render, non-focusable, DOM order, flag-met unlock, 0 console errors) → **Codex impl pass** (1 Major + 2 Minor, all folded — soft-lock warning generalised to `isGuaranteedActionable` over scenes + authored rounds + reaction-replaced rounds, false positives on `shown`/ungated-escape fixed, warning taxonomy documented). Pushed branch, opened **PR #86** (merge commit, never squash).
- **Verified:** on `2b0dcdb`: `npm run test:run` → **816 passed (816)** / **82** files (was 766/78, +50); `npm run lint` clean; `node scripts/validateCase.mjs` → **8 cases, zero errors, zero warnings**; `npm run build` green. Live browser verify via Playwright (edited v5 save to reach `cc-act2-hub` gate-unmet and gate-met).
- **Open / blockers:** **Merge PR [#86](https://github.com/JonWhiteFang/gaslight-and-grimoire/pull/86)** — completes the entire UI/UX roadmap (Phases 0–5). Then content resumes: The Orrery Room, authoring with the new vocabulary from day one. Unchanged: #20 (media, user-blocked). Future-touch note: `getEncounterChoices` could return the shown/locked partition (whole-branch Minor 3).
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no standalone decision — Phase 5 enacts the existing roadmap item; the four design forks live in the Codex-reviewed spec). **Sweep:** `status.md` baseline 766/78 → **816/82** (date 07-16 → 07-17); `ui-ux-roadmap.md` Phase-5 row → ✅ (PR #86); `ui-ux-improvements.md` backlog item 8 → ✅. T8 already fixed the in-scope doc drift (stale `isChoiceVisible` reference in content-authoring.md; validator warnings taxonomy).

## 2026-07-17 — Phase 4 MERGED (PR #85) + post-merge checkpoint

- **Goal:** Merge the completed Phase 4 a11y-sweep branch and sync `main`; refresh the spine from "PR #85 open" to "merged".
- **Did:** Verified all 6 CI checks green on PR #85, merged via `gh pr merge 85 --merge` (merge commit `8e1c2b0`, never squashed). Synced local `main` (`git pull --ff-only`), deleted the merged branch local + remote. No code touched this session — docs/spine only.
- **Verified:** on `main` at `8e1c2b0`: `npm run test:run` → **766 passed (766)** / **78** files. CI on PR #85: all 6 checks pass (test, build, npm-audit, OWASP, Cloudflare Workers Build, dependency-check).
- **Open / blockers:** None. UI/UX track complete through Phase 4. Next code work: **Phase 5 (choice-gating content model)** — the last roadmap item — or content resumes with **The Orrery Room** (Grey Dawn vignette). No branch open. Unchanged: #20 (media, user-blocked). Minor pre-existing lint-clean TS hint (`React` unused in TitleScreen/CaseSelection/VitalityMeter) noted for a trivial future cleanup.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no decision — merge + sync only). **Sweep:** PROJECT_STATE `_Last updated:_` → 2026-07-17 (merge line prepended; the 07-16 implementation line kept as prior-update record); U4 tracker row `open`→**MERGED (`8e1c2b0`)**; Branch-focus updated (was stale on `feat/phase3-dice-legibility`) → none/`main`; Next-actions "merge PR #85" → track-complete. Also fixed one adjacent stale line caught in the sweep: the **Stage** line still said "8 cases once PR #76 merges; 7 on `main` today" — PR #76 merged long ago, `main` has 8 cases; corrected.

## 2026-07-16 — Phase 4 IMPLEMENTED — a11y hardening sweep shipped (PR #85)

- **Goal:** The last scheduled UI/UX phase — an accessibility hardening sweep against WCAG (roadmap items 3/4/5 + the "preserve" regression tests): reduced-motion coverage, focus-restore + background inertness across all overlays, and a contrast/focus-indicator pass. Verify-and-close-gaps, components/CSS/tests only.
- **Did:** Brainstorm → spec → **file-based Codex spec review** (4 Major + 3 Minor, all folded — incl. **Major 1: title-screen Settings had no `inert` wrapper (real bug)**; the two `amber` tokens are distinct so pick the ring token explicitly; enumerate every reduced-motion Motion gate; SC 2.4.11 mis-cite → 2.4.7+1.4.11) → plan → **Codex plan review** (6 Major + 1 Minor, all folded — mock framer-motion to guard every gate; migrate ALL bare-focus rings + source-inventory test; resolved-token contrast; inert-during-Suspense-fallback test; SettingsPanel Escape→window; `undefined` not `null` for the ThreadPoint typecheck). Executed 9 tasks via **subagent-driven-development** (fresh implementer + spec + quality reviewer per task): **T1** SettingsPanel → shared `useFocusTrap` (Escape→window, focus-restore); **T2** title-screen `inert={isSettingsOpen}` (gated on state so it holds during the Suspense fallback); **T3** consumer focus-restore guards (3 overlays); **T4** direct reduced-motion guards for every Motion gate via a framer-motion prop-capture mock + structural CSS guard (`readFileSync`, since `?raw` is empty under Vitest) + mutation-checks (4 quality fixes folded on review); **T5** ALL ~11 components' keyboard focus rings → `focus-visible:ring-2 ring-amber-400` + glob-driven tree-wide inventory test + resolved-oklch contrast table (amber ring 9.90:1 on ink); the InvestigationHalted outlier caught + migrated, glob-hardening + ratio fix folded on review; **T6** preserve tests (save-toast roles + failure `aria-live`, keyboard connect, dice-as-status); **T7** full gate; **T8** live in-browser verify. **Whole-branch integration review then caught a real WCAG 2.4.3 bug** the per-task jsdom reviews structurally couldn't see: the `inert` ancestor blurs the invoker to `<body>` before `useFocusTrap` captures it, so ALL overlays (pre-existing, app-wide) restored focus to `<body>` — and made the standalone "restore" tests false-greens. **Fixed (T9b):** capture the invoker at OPEN time (`invokerRef` in App, before `setState`/inert) and thread `restoreFocusTo` to the hook + all 5 render sites; browser-confirmed focus now returns to the trigger. **File-based Codex impl pass (T9c):** 3 findings, all test-quality false-green fixes (production confirmed sound) — real focus-restore integration guard that *simulates* inert's blur (jsdom doesn't), complete reduced-motion coverage (VitalityMeter/Hint-trigger/transition branches), hardened ring inventory (`FILES.length>0`, both `focus:`/`focus-visible:` scan, documented non-amber exceptions). Opened **PR #85** (merge commit, never squash).
- **Verified:** on `591d9b2`: `npm run test:run` → **766 passed (766)** / **78** files (was 733/74, +33); `npm run lint` clean; `node scripts/validateCase.mjs` → **8 cases** clean; `npm run build` green. Live-verified via Playwright: amber focus ring visible on dark + high-contrast; title inertness blocks background focus; **closing Settings restores focus to the trigger, not `<body>`** (confirmed the pre-fix path landed on `<body>`); `.reduced-motion` zeroes CSS `animation/transition-duration`; 0 console errors.
- **Open / blockers:** **Merge PR [#85](https://github.com/JonWhiteFang/gaslight-and-grimoire/pull/85)** (merge commit). No blockers. Unchanged: #20 (media). After Phase 4: **Phase 5 — choice-gating content model** is the last roadmap item (consider pulling forward if content resumes).
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new decision — the focus-restore fix was a bug fix with an obvious approach, not an alternative-bearing architectural call; the phase enacts existing roadmap decisions). **Sweep:** `status.md` test baseline 733/74 → **766/78**; `ui-ux-roadmap.md` Phase-4 row `[ ]`→`✅` (PR #85, + the focus-restore-bug note); `ui-ux-improvements.md` "Do now" items 2/3/4/5 → ✅ (item 2 = Phase 2a; 3/4/5 = this PR). PROJECT_STATE header + new U4 tracker row + Verification + Next-actions all refreshed.

## 2026-07-16 — Phase 3 IMPLEMENTED — dice/probability legibility shipped (PR #84)

- **Goal:** Make faculty-check odds legible before and after the roll (DC + a diegetic "Prospects" band + advantage) without ever showing a literal %, across all three check surfaces — the UI/UX roadmap's Phase 3.
- **Did:** Brainstorm → spec → **file-based Codex spec review** (5 findings, all folded — auto-succeed "Assured" treatment, surface-dependent partial policy, accessible-name-not-nested-tag, encounter scope correction, real-check guard) → plan → **Codex plan review** (9 findings, all folded — incl. the overlay-DC never wired to the render call, invalid `detective` fixtures, weak clamp/boundary tests, vacuous Task-6 GREEN, non-reactive encounter flags). Executed 8 TDD tasks via **subagent-driven-development** (fresh implementer + spec + code-quality reviewer per task): **T1** shared pure `checkAutoSucceeds` predicate (resolver refactor, behaviour-preserving); **T2** pure `checkOdds.ts` — `computeCheckOdds` band classifier (`p = clamp((21-needed)/20, 1/20, 19/20)`, adv `1-(1-p)²` / disadv `p²`, ≥.65/.35 thresholds) + `describeCheckOdds` accessible phrase, RNG-free, exact-boundary + clamp-flip + monotonic property tests; **T3** `CheckResult.dc?` (transient) + `DiceRollOverlay` "vs DC N" + wired through the `NarrativePanel` render call; **T4** decorative `aria-hidden` `CheckOddsTag` (renders no ◈ glyph; literal "Prospects:" label); **T5** `ChoiceCard` renders the tag + folds odds into its **own** button `aria-label` + real-check guard + `autoSucceeds` prop; **T6** wired `ChoicePanel`/`EncounterPanel` (reactive `useStore(s=>s.flags)`, `dc` into `setCheckResult`) + strengthened tests (click-level `lastCheckResult.dc`, post-mount reactivity); **T6b** extracted a single `isFacultyCheck(choice)` type guard replacing ~6 drifted spellings across ChoiceCard/ChoicePanel/EncounterPanel/choiceResolution/contentValidation (the roll and odds now can't disagree); **T7** `SceneCluePrompts` DC + tag (`partialCountsAsSuccess: true`, `autoSucceeds: false` hard-coded — that path doesn't consume the ability) + `dc` forwarded through `NarrativePanel`; **T8** docs (engine-reference/architecture/status). Then **whole-branch review** (verdict Ready to merge) → **live browser verify** (Playwright: Prospects band across 5 faculties, odds in each button's accessible name, non-check choices show no DC, and **"Assured"** rendering for a Reason check with the Elementary ability active — beside an ordinary Nerve band; 0 console errors) → **file-based Codex impl pass** ([review](../codex/output/2026-07-16-phase3-dice-legibility-impl-review.md)): 2 Majors, both folded — (1) choice partial-tier policy was a blanket `false` but ~43% of shipped faculty choices route `partial` to the success/critical scene, so `ChoiceCard` now derives `partialCountsAsSuccess` from `outcomes.partial === outcomes.success||critical`; (2) `loadGame` didn't clear `lastCheckResult`, leaking a stale roll/DC overlay onto a loaded save → now cleared. Opened **PR #84** (merge commit, never squash).
- **Verified:** on `d121e9b`: `npm run test:run` → **733 passed (733)** / **74** files (was 684/64, +49); `npm run lint` clean; `node scripts/validateCase.mjs` → **8 cases** clean; `npm run build` green. Live-verified in-browser via Playwright.
- **Open / blockers:** **Merge PR [#84](https://github.com/JonWhiteFang/gaslight-and-grimoire/pull/84)** (merge commit). No blockers. Unchanged: #20 (media). After Phase 3: Phase 4 (a11y hardening sweep) is the last scheduled UI/UX phase.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new decision — Phase 3 surfaces odds under the already-Accepted ADR-0012 roll-semantics; no alternative-bearing architectural call this session). **Sweep:** `status.md` baseline 730→**733** (the impl fixes added 3 tests after T8's doc commit set 730); `ui-ux-roadmap.md` Phase-3 row `[ ]`→`✅` (PR #84); spec §3.1/§4.1 corrected — the "choices: partial always distinct" premise was false, now data-driven per impl-review Major 1. PROJECT_STATE header + U3 tracker row + Branch-focus + Verification + Next-actions all refreshed.

## 2026-07-16 — Phase 2b IMPLEMENTED — deduction formation model shipped (PR #83, enacts ADR-0012)

- **Goal:** Implement the 8-task Phase 2b plan (deduction formation rework enacting ADR-0012) end-to-end, through review + PR.
- **Did:** Executed the plan TDD on `feat/phase2b-deduction-formation`. **T1** new pure `src/engine/deductionOracle.ts` (`classifyBoard` — union-find over player edges, recipes matched against player topology not `connectsTo`, generic path against undirected `connectsTo`, forms EVERY matched recipe [Blocker 1], fail-closed on missing/unrevealed/inherited/self edges) + `DeductionCorrectness`/`ClassifiedComponent` types. **T2** `buildDeduction` canonical stable id `deduction-generic-<sorted ids>` (no `Date.now`/`Math.random`, N5). **T3** validator reserves the `deduction-generic-` recipe-id namespace + enforces clue-id `^[a-z0-9-]+$` (Major 4). **T4** store-owned contested-revert — module `revertTimers` registry, `contestedTokens`/`contestedPrior`/`attemptSeq`, `contestClues`/`markCluesDeduced`/`cancelContestedReverts`/`clearRevertTimers`; `narrativeSlice.resetForNewCase` + load actions + `metaSlice.loadGame` cancel pending reverts (N2/Blocker 2; baseline-prior carry-forward for fail→fail). **T5** save `CURRENT_SAVE_VERSION` 4→5 — v4→v5 `connected`→`deduced`/`examined` recovery + `contested`→`examined` hygiene on **every** load path (Major 3). **T6** `ClueCard isConnected` prop drives ring + 🔗, `connected` removed from status rendering (N1). **T7** board runs the oracle + forms all qualifying deductions + drives statuses/banner/announce; `DeductionButton` rolls only (`onResult(tier)`, no sticky lock, Major 5); ADR-0012 Confirmation tests land here. **T8** ADR-0012 `Accepted → Enacted` (front matter + Confirmation + Links + README row, body untouched per MADR); engine-reference/architecture/content-authoring/CLAUDE.md updated. Then **internal whole-branch review** (verdict Ready to merge; 1 real Minor folded — mark only deduction-member clues `deduced`, not lassoed noise) → **live browser verify** (correctness-gated formation, derived cue on a `deduced` clue, baseline-prior revert, repeat attempts, 0 console errors) → **file-based Codex impl pass** ([review](../codex/output/2026-07-16-phase2b-deduction-formation-impl-review.md)): 3 findings all folded — 1 Major (null clue value threw in the new migration traversal → guarded both loops + `load()` try/catch + `isValidGameState` rejects non-object entity values) + 2 Minor (slack only failed components' edges; count-bearing banner when >1 component evaluated). Opened **PR #83** (merge commit, never squash).
- **Verified:** on `4e2412d` (impl) then review fixes: `npm run test:run` → **684 passed (684)** / **64** files (was 649/61, +35); `npm run lint` clean; `node scripts/validateCase.mjs` → **8 cases** clean; `npm run build` green (incl. `typecheck:scripts`). Live-verified in-browser via Playwright.
- **Open / blockers:** **Merge PR [#83](https://github.com/JonWhiteFang/gaslight-and-grimoire/pull/83)** (merge commit). No blockers. Unchanged: #20 (media). After 2b: Phase 3 (dice legibility) or Phase 4 (a11y sweep).
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no NEW decision — this session *enacts* ADR-0012, promoted `Accepted→Enacted` in T8; the member-only-`deduced` refinement is a review fold within that model, not a standalone ADR). **Sweep:** `status.md` baseline 649/61 → 684/64; PROJECT_STATE header + U2b row `[~]`→`[x]` + Branch-focus + Verification + Next-actions all refreshed to PR-open. Stale ADR-0012 "Accepted/NOT enacted" mentions elsewhere are point-in-time prior-update/spec records — left as-is per the immutability rule.

## 2026-07-16 — Phase 2b deduction formation model: spec + plan written & Codex-reviewed (no code yet)

- **Goal:** Turn the deferred Phase 2b analysis (Phase 2 spec Part B) into an approved spec + implementation plan for the deduction **formation** rework that enacts ADR-0012.
- **Did:** Docs-only, on new branch `feat/phase2b-deduction-formation` (off `1e64758`). Brainstorm → resolved **four design forks** (all toward the root-cause option): (1) `'connected'` derived from membership, not a written status; (2) partial-correctness forms nothing + amber directional copy; (3) evaluate each connected component independently; (4) a red-herring clue in an all-authored cluster forms an *uneasy* deduction. Verified the load-bearing corpus facts against `main` before writing: 7 recipes / 4 vignettes ship none; **2 of 7 recipes (`lw-`, `ms-`) are NOT `connectsTo`-connected** → recipes must match player topology; all clue ids `^[a-z0-9-]+$`; single `'connected'` writer. Wrote the **[spec](superpowers/specs/2026-07-15-phase2b-deduction-formation-design.md)** (pure `deductionOracle.ts` two-graph classifier → `correct`/`false`/`partial`/`incorrect`; board forms every qualifying deduction; store-owned contested-revert; canonical stable generic id; save v4→v5). **Codex spec pass** ([review](../codex/output/2026-07-15-phase2b-deduction-formation-review.md)): 5 findings, all folded — 2 Blockers (form ALL matching recipes not one; store-owned timer ownership) + 2 Major (migration `connected→deduced` recovery; reserve the `deduction-generic-` id namespace) + 1 Minor (empty-result guard). Then the **[plan](superpowers/plans/2026-07-16-phase2b-deduction-formation.md)** (8 TDD tasks) → **Codex plan pass** ([review](../codex/output/2026-07-16-phase2b-deduction-formation-plan-review.md)): 5 more findings, all folded — 2 Blockers (the success-path token claim was prose not code → concrete atomic `markCluesDeduced`; timers untracked → module-level registry + `cancelContestedReverts` on `resetForNewCase` AND `metaSlice.loadGame`) + 3 Major (fail→fail strand → baseline-prior carry-forward; v5 save reloads stuck `contested` → `contested→examined` hygiene on every load path; button sticky-locked after success → drop terminal phase). Codex independently re-verified the corpus facts both rounds; it validated the oracle/validator/generic-id design as sound — every defect was in the revert/timer lifecycle.
- **Verified:** n/a — docs only, no code touched. Baseline holds **649/61** (nothing run; nothing to run). The spec's corpus claims WERE verified live this session (node one-offs over `public/content`: recipe counts, the 2 non-`connectsTo` recipes, clue-id charset).
- **Open / blockers:** **Implement the 8-task plan** via subagent-driven-development (oracle → generic id → validator → store revert → save migration → ClueCard → board integration → enact ADR-0012 + docs), then whole-branch review + live verify + **impl Codex pass** → PR (merge commit, never squash). No blockers. Unchanged: #20 (media).
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new decision — this work *enacts* the existing ADR-0012, promoted `Accepted→Enacted` during Task 8 of implementation; the four resolved forks live in the spec). **Sweep:** roadmap Phase-2b row `[~]` + spec/plan links + "spec ✅ plan ✅ impl pending"; PROJECT_STATE U2b row `[ ]`→`[~]`; refreshed Branch-focus (was still `docs/comet-club-spec`/PR #76 — pre-existing drift), Verification line (+docs-only 2026-07-16 note), Next-actions (Phase 2b leads); corrected the stale "merge PR #76" content-track line (merged 2026-07-12).

## 2026-07-15 — Codex review → file-based (ADR-0013) + Phase 2a deduction legibility (PR #82 merged)

- **Goal:** Start UI/UX Phase 2 (deduction feedback legibility); mid-session, switch the Codex review procedure to a file-based handoff.
- **Did:** Two strands, one merged PR (#82, merge commit `63e4442`, never squash). **(1) Codex procedure change:** user removed the `codex` MCP server (user scope) and supplied a new procedure — a **file-based** handoff at **three** checkpoints (spec, plan, completed implementation) via `codex/input`→`codex/output`; rewrote CLAUDE.md §"Cross-provider review with Codex", wrote **[ADR-0013](DECISIONS/ADR-0013-codex-file-based-review-handoff.md)** superseding **ADR-0010** (front-matter `superseded-by` + preserved banner per MADR immutability; README index + PROJECT_STATE de-drifted). **(2) Phase 2a — deduction feedback legibility (roadmap items 6+2):** brainstorm → spec. **The spec took THREE Codex review rounds, each finding a real Blocker:** round 1 killed the tag-overlap correctness oracle (tags ≠ authored relationships; a live red-herring recipe gates a branch; the button unmounts and loses its message); round 2 killed the recipe-only oracle (no topology on the flattened id-union; **deduction impossible in all 4 vignettes** which ship no recipes; partial became a membership-farm; stale/destructive status revert); round 3 traced the latest Blocker to **pre-existing board plumbing** (clues overwrite status to `connected` on connect, before any attempt). Resolved by **splitting into 2a (ship now) / 2b (deferred)**. Built 2a via subagent-driven dev (4 tasks, per-task spec+quality reviews): `connected` 🔗 cue (WCAG 1.4.1); the outcome message moved off `DeductionButton` (whose subtree vanishes when connections clear) onto the always-mounted `EvidenceBoard`, announced once via the Phase-1 `announce()`; directional partial-tier copy. **Legibility-only: formation unchanged, ADR-0012 NOT enacted.** Plan Codex pass (5 findings folded in); impl Codex pass caught **1 Major** (banner clipped the close button off a 375px viewport — the header was one non-wrapping flex row) → fixed as a full-width row below the toolbar, **live-verified at 375px** (close button stays in-viewport). Live-verified the whole flow via Playwright: 🔗 badge renders; deduction forms; app-root live region announces the tier-flavoured message.
- **Verified:** on merged `main` — `npm run test:run` → **649 passed (649)** / **61** files (was 635/60; +14); `npm run lint` clean; `node scripts/validateCase.mjs` → 8 cases clean; `npm run build` green. CI on PR #82 all green **except** OWASP Dependency-Check, which failed on an external **NPM Audit API outage** (not a vulnerability, not a required check, zero dep changes in the PR — re-run failed identically) — merged per explicit user direction.
- **Open / blockers:** **Phase 2b** is the deferred formation model (recipe/`connectsTo` two-graph oracle over player topology; enacts ADR-0012; fixes the pre-existing `contested`-revert latent bug) — full analysis preserved in the Phase 2 spec's Part B; needs its own spec → file-based Codex reviews. Or pick Phase 3/4 per the roadmap. Unchanged: #20 (media).
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☑ ([ADR-0013](DECISIONS/ADR-0013-codex-file-based-review-handoff.md)). **Sweep:** roadmap Phase-2 row split into 2a `[x]` / 2b (todo); PROJECT_STATE Verification line extended to 649/61 + the Phase-1 635/60 step it had missed; C1 (Comet Club) flipped `[~]`→`[x]` (PR #76 long merged — pre-existing drift); added tracker rows U1/U2a/U2b; fixed the ADR-0013 date (07-14→07-15) in the Codex-gates row. status.md already at 649/61.

## 2026-07-14 — UI/UX track opened + Phase 1 (global live announcer) shipped

- **Goal:** Deep-research how to improve the game's UI/UX, turn it into a sequenced roadmap, and execute the first phase.
- **Did:** Three threads. (1) **Research (PR #79, merged):** ran the deep-research harness (5 angles → 24 verified sources) → [`docs/research/ui-ux-improvements.md`](research/ui-ux-improvements.md); a Codex review caught that the report's premises were untested against code, so restructured it into *research findings* + a *code-checked repo audit* (corrected: evidence board is click-select not drag-heavy → WCAG 2.5.7 already met; dice overlay/save/case-select are status/toast/route surfaces, not modals). Added track-C roadmap [`ui-ux-roadmap.md`](research/ui-ux-roadmap.md) and **[ADR-0012](DECISIONS/ADR-0012-deduction-roll-semantics.md)** (Phase-0 decision: a recipe-matching clue-set always forms its deduction; the Reason d20 flavours the tier, no longer gatekeeps correct reasoning — Accepted, enacts in Phase 2). (2) **Phase 1 (PR #80, open):** brainstorm → design spec → **design Gate 1** (not-sound-as-is; 5 findings folded in: mount in `main.tsx` not `AccessibilityProvider`, readiness+queue, dropped the store-subscription as non-additive, two-slot re-announce not a zero-width nonce, cached snapshot) → implementation plan → **plan Gate 1** (4 findings) → **subagent-driven execution** (4 tasks, fresh implementer + spec+quality reviews each) → **code Gate 2** (round 1: a real remount bug — module-level `ready` let a remount commit non-empty regions [Med] + a test-coverage gap [Low], both fixed with a per-mount `regionsCommitted` gate; round 2 verdict "sound to merge as-is"). Shipped `src/announcer.ts` + `src/components/LiveAnnouncer/` + a 5-line `main.tsx` mount; no existing `aria-live` region touched. (3) **Tooling:** established a committed `codex/` handoff dir (`input/` prompts, `output/` reviews — Codex writes only there); documented the pattern in `CLAUDE.md` (the `codex exec`/MCP path kept timing out at xhigh effort).
- **Verified:** on the branch HEAD — `npm run test:run` → **635 passed (635) / 60 files** (was 611/58; +15 announcer + 9 LiveAnnouncer); `npm run lint` clean; `node scripts/validateCase.mjs` → 8/8 cases; `npm run build` green. Additive-scope guard: `git diff main` shows only `announcer.ts`, `LiveAnnouncer/*`, `main.tsx` in `src/`, and no `aria-live`/`role` line changed elsewhere.
- **Open / blockers:** PR #79 (docs) merged; merge PR #80 (Phase 1) — merge-commit, never squash. **On #80 merge:** the roadmap Phase-1 row still says the announcer lives in `AccessibilityProvider` (a pre-Gate-1 guess; actual mount is `main.tsx`) and `ui-ux-improvements.md` still marks the global announcer ⭕ absent — left unedited because they're only stale *after* merge; flip both then. Next build: Phase 2 (deduction feedback legibility) — consumes `announce()`, enacts ADR-0012.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (ADR-0012 was authored at last session's decision point and merged via PR #79; no new decision this session — Phase 1 executed an already-approved design). Sweep: bumped `status.md` test baseline 611/58 → 635/60; the `611`/`58` hits in `PROJECT_STATE.md` are historical RUN_LOG-style entries, left as point-in-time records.

## 2026-07-12 — The Comet Club authored: fourth main case, flagship scale (PR #76)

- **Goal:** Build a new main case from the ideation doc's ranked pick (user chose The Comet Club, flagship scale, hybrid A+B expansion).
- **Did:** Full pipeline on branch `docs/comet-club-spec`: brainstormed design (spec committed `84d7d49`) → implementation plan → **Codex Gate 1** (round 1: 3 blockers — sceneSource ordering, Influence-only diary route, best ending not requiring the unmasked murderer — + 6 more, all fixed; round 2: 3 small findings, fixed) → **subagent-driven execution** of 7 tasks (fresh implementer per task, per-task spec+quality reviews; 3 fix cycles across the run) → content-integrity review (0 blockers; numbered-tickets deduction wired to a payoff variant) → whole-branch review (2 Important fixed: `mythos-period-computed` mis-set on the blind eyepiece path; Reason retags. Recommendation to gate Act III on a deduction rejected — deduction = Reason roll = RNG gate) → **Codex Gate 2** (1 MAJOR: a fix subagent *reported* removing the flag but had only duplicated it — verified by grep, fixed directly `c967878`; round 2 PASS). Case: **75 scenes (67 base + 8 variants) / 16 clues / 10 NPCs / 4 deduction recipes**; catalog now 8 cases / 276 scenes / 74 clues / 40 NPCs. Pushed + **PR #76** opened (merge-commit only). ADR-0011 records the pick, scale, flag-semantics and rejected-alternative decisions. **Sweep:** fixed CLAUDE.md "7 cases (3 main…)" → "8 cases (4 main…)"; status.md was updated in-branch during the build (row + totals + two stale prose counts). Lesson recorded: subagent fix reports can be wrong — Gate 2 caught a misreported one-line fix; verify flag/effect placement by grep, not by report.
- **Verified:** on `c967878`: `npm run test:run` → 611 passed (611) / 58 files; `npm run lint` clean; `node scripts/validateCase.mjs` → all 8 cases, zero errors **and zero warnings** (the-comet-club 75 scenes / 16 clues). Content-integrity reviewer ran this session (Task 8) — gate satisfied.
- **Open / blockers:** merge PR #76 (merge commit, never squash — user action). Three bookkeeping flags set but unread (`cc-tonic-dissolved`, `cc-ost-confided`, `cc-midpoint-passed`) — wire or drop next content session. Accepted deviations: Mesmerist exclusive fires after a lost pursuit; Hampstead summons is an unmarked one-way door. Next build: The Orrery Room (consumes `mythos-period-computed`). Unchanged: #20 (media).
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☑ (ADR-0011)

## 2026-07-11 (fourth session) — context7 MCP scope conflict fixed (first task through the ADR-0010 gates)

- **Goal:** Resolve the `claude mcp list` diagnostic flagged last session: `context7` defined at user scope (HTTP `https://mcp.context7.com/mcp`) *and* project scope (stdio `npx -y @upstash/context7-mcp`) with different endpoints.
- **Did:** Kept the repo-shareable project scope (per ADR-0004's team-shareability intent) but standardized it on the HTTP endpoint: `.mcp.json` context7 entry → `{"type": "http", "url": "https://mcp.context7.com/mcp"}`; then `claude mcp remove context7 -s user` (machine-local `~/.claude.json`). **ADR-0010 gates exercised for real:** Gate 1 found an ordering hazard (plan originally removed the working user entry before proving the project entry parsed/connected — reordered to edit → JSON-validate → remove → probe, with the diff-hygiene check it also asked for); Gate 2 on the final diff: "no blocking findings." Verified: conflict diagnostic gone; `claude mcp get context7` → Scope: Project, Type: http, correct URL. Sweep: no doc drift (docs name context7 without transport specifics).
- **Verified:** `python3 -m json.tool .mcp.json` valid; `git diff` touches only the context7 stanza (playwright/github untouched); post-removal `claude mcp list/get` probe. Accepted residuals (surfaced by Gate 1, not fully closable here): project-scope servers show a one-time "Pending approval" trust prompt on next launch (standard; same as the repo's playwright/github entries), and no fresh-clone smoke test was run (the identical URL is proven reachable in the same listing). Repo change is one config stanza; test baseline unaffected (611/58, 201/7 stands).
- **Open / blockers:** approve the context7 project server at next interactive launch. `npm ci` before next code session. Unchanged: #20 (media), content-ideas pick.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (routine config fix — no decision with lasting alternatives; the scope-choice rationale already lives in ADR-0004).

## 2026-07-11 (third session) — Codex integrated as cross-provider adversarial reviewer (ADR-0010)

- **Goal:** Execute the user's hardened setup prompt: OpenAI Codex CLI as an MCP-based adversarial reviewer, with every non-trivial plan and code change gated on a Codex review (or an explicitly announced skip).
- **Did:**
  - **Preflight found the machine-side already done:** Codex CLI 0.144.1 (brew cask), ChatGPT auth (no stored API key), `codex` MCP server at **user scope** `✔ Connected`, and `~/.codex/config.toml` already carrying the effective top-level `sandbox_mode = "read-only"` + `approval_policy = "untrusted"` (confirmed `codex doctor`: approval `UnlessTrusted`, filesystem sandbox `restricted`). Model `gpt-5.6-sol` smoke-tested working on ChatGPT auth (`codex exec --sandbox read-only ... < /dev/null`) — the prompt's expected model blocker never applied. MCP tools were already loaded in-session, so the usual "restart Claude Code" open item didn't apply either. **No machine config was changed.**
  - **Repo-side (the only change):** appended CLAUDE.md §"Adversarial review with Codex" — Gate 1 (every non-trivial task's plan reviewed **before any mutation**, attached to the work not to a declared plan) + Gate 2 (**complete task diff vs. recorded starting base**, untracked files `git add -N`'d, reviewed before completion; runs before `/checkpoint`, mechanical spine updates exempt) + minimum-context and two-round/escalation rules. **The gate text was itself dogfooded through both gates:** Codex's round-1 review of the verbatim prompt text found 5 loopholes (uncommitted-only diffs, undeclared-plan dodge, accepted-but-unfixed findings, checkpoint ordering, vague context); user approved the hardened variant; round-2 re-review confirmed 4 fixed + 1 residual (untracked files) which was fixed with Codex's exact wording. Gate-1 live test also produced a real repo-specific catch (faction key must be the exact string `Hermetic Order of the Grey Dawn`).
  - **[ADR-0010](DECISIONS/ADR-0010-codex-adversarial-review-gates.md)** (Enacted) records the decision; indexed; STATE updated. **Doc-drift sweep fix:** `docs/README.md`'s CLAUDE.md blurb extended to mention the review gates.
  - **Replicate machine-side on another machine:** `brew install --cask codex && codex login && claude mcp add --scope user codex -- codex mcp-server`, then top-level `sandbox_mode = "read-only"` / `approval_policy = "untrusted"` in `~/.codex/config.toml`.
- **Verified:** `codex doctor` (policy/sandbox/auth), CLI smoke test (model answered, 10.5k tokens), and both gates exercised live through `mcp__codex__codex` (plan review + diff review, read-only). Repo change is docs-only; 2026-07-09 baseline (611/58, 201/7) stands (`node_modules` still absent).
- **Open / blockers:** `npm ci` before next code session. Flagged, unresolved (user's call): `claude mcp list` reports a **context7 scope conflict** (user-scope HTTP endpoint vs. project `.mcp.json` npx entry). Unchanged: #20 (media), content-ideas pick.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☑ ([ADR-0010](DECISIONS/ADR-0010-codex-adversarial-review-gates.md))

## 2026-07-11 (second session) — CLAUDE.md trimmed to a pointer map (ADR-0009, docs-only)

- **Goal:** The user asked for a sweep of CLAUDE.md ("anything to cut or refine?"). A claude-md-improver audit scored it 78/100 (B): commands/warnings strong, but ~40% of 339 lines restated `docs/`-owned facts — the proven source of the recurring sweep drift (17 CLAUDE.md commits since 2026-07-07: 198→201 scenes, React 18→19, save v3→v4, …). User first chose report-only, then approved the **full trim**.
- **Did (docs-only — no code, no `public/content/`):**
  - **CLAUDE.md 339 → 165 lines.** Cut to pointers: Engine Behaviour (→ `engine-reference.md`), Component Hierarchy (→ `architecture.md`), Current Content (→ `status.md`, plus a "verify counts with the validator, don't quote from memory" instruction), Key Types (→ `types/index.ts` + `content-authoring.md`), store slice table (Rules bullets kept), Content Authoring Rules restatements (three non-negotiables kept). Deleted: completed A–E Implementation Roadmap, Character System, Tailwind Theme, the duplicate onEnter/visitedScenes explanation (Architectural Warnings copy kept). Kept in full: doc map (now stating one-authority-per-fact explicitly), memory-spine protocol, two-domain rule, Directory Layout (annotations condensed), Commands, Testing, CI/CD (incl. no-squash), all 8 Architectural Warnings.
  - **[ADR-0009](DECISIONS/ADR-0009-claude-md-pointer-doctrine.md)** (Enacted, first ADR on the new MADR 4.x template): CLAUDE.md is a pointer map, not a mirror — reference detail lives only in its authoritative doc; the sweep's duplication check now treats restated `docs/` facts in CLAUDE.md as drift. Indexed in `DECISIONS/README.md`; linked from STATE → References.
  - **Doc-drift sweep (fixes):** two references into removed sections repointed — PROJECT_STATE's tracker legend cited "the Implementation Roadmap in ../CLAUDE.md" (→ `status.md`); `docs/README.md`'s CLAUDE.md blurb listed "known gaps" (→ actual current contents). No anchor links into removed sections found; design bible's own "Character System"/"Roadmap" headings are its own content, untouched.
- **Verified:** n/a — docs-only; no code/content changed. Grep-verified no dangling references to removed sections. Validator/tests still not runnable locally (`node_modules` absent); 2026-07-09 baseline (611 tests / 58 files, 201 scenes / 7 cases) stands.
- **Open / blockers:** `npm ci` still needed before the next code session. Unchanged: #20 (media), content-ideas pick pending.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☑ ([ADR-0009](DECISIONS/ADR-0009-claude-md-pointer-doctrine.md))

## 2026-07-11 — Memory-spine sync to the updated portable guide (docs/config only)

- **Goal:** The upstream `guides/portable-memory-spine.md` (JonWhiteFang/Documents, private repo — fetched via `gh api`) took two updates today: a hardening pass (commit `0cadf2b9`) and a MADR 4.x ADR-template modernization (`e77d4eed`). Bring this repo's spine implementation (adopted in [ADR-0002](DECISIONS/ADR-0002-committed-memory-spine.md)) in line.
- **Did (14 files, docs/`.claude` config only — no code, no `public/content/`):**
  - **MADR 4.x:** rewrote `DECISIONS/ADR-TEMPLATE.md` — YAML front matter (`status/date/deciders/phase/supersedes/superseded-by`) + new **Confirmation** section; script-converted all 8 existing ADRs' metadata bullets to the same front matter (values verbatim, YAML-parse-verified; decision bodies untouched); `DECISIONS/README.md` gained front-matter "How to add" steps + the precise immutability rule (decision content frozen; front matter/Confirmation/Links/index row mutable).
  - **Hardening fixes:** "append-only" → "prepend-only" RUN_LOG terminology (this file's header, `docs/README.md`, `CLAUDE.md`); `/checkpoint` skill: step order → sweep → ADR → STATE → RUN_LOG → report, supersessions via front matter + index, commit-expectation added to step 6, guardrails gained the report-only sweep variant + branch/multi-agent conflict protocol. Hook + settings already matched the guide — unchanged.
  - **Doc-drift sweep:** clean — no other doc restates the spine mechanics; all links in changed files resolve. Deliberate leave-alone: ADR-0002's body says "append-only" (frozen point-in-time record; not a superseding decision, so no front-matter/index annotation either).
- **Verified:** n/a — no code/content changed; YAML front matter of all 8 ADRs parse-checked with PyYAML. Validator/tests not runnable (`node_modules` still absent); 2026-07-09 baseline (611 tests / 58 files, 201 scenes / 7 cases) stands.
- **Open / blockers:** `npm ci` still needed before the next code session. Existing ADRs lack Confirmation sections (template-only addition; retrofitting would mean inventing content — new ADRs get them). #20 (media) unchanged; content-ideas pick still awaiting the user.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new decision — this syncs the spine to the guide it already adopted via ADR-0002; the retrofit-vs-leave metadata call was cosmetic).

## 2026-07-10 — Content ideation: 10 new case/vignette concepts + Mythos-thread staging (docs-only)

- **Goal:** Execute `docs/content-ideas-prompt.md` (added last session) — generate 10 new content concepts (6 main cases + 4 vignettes) complementing the existing catalog, rank the top 3, pitch the winner in detail, and stage an optional cosmic-horror Mythos thread across them. Then checkpoint.
- **Did (docs-only — no code, no `public/content/` touched):**
  - Read the required context (README, design bible, content-authoring guide, manifest, all 7 `meta.json`s) and wrote **[`docs/content-ideas-2026-07-10.md`](content-ideas-2026-07-10.md)**: 10 fully-fielded concepts (synopsis, mystery question, rational shadow, occult truth, locations, factions, NPCs, typed clue ideas, key deductions, branch choice, faculty emphasis, archetype hooks, flags/unlocks, novelty rationale each). Top 3: **The Comet Club** (closed circle, double solution — one human tontine murder inside an inhuman pattern), **The Ravenscroft Remedy** (activates the unused Ravenscroft Asylum; the "cure" that excises Veil-memories), **The Orrery Room** (fills the missing **Grey Dawn faction vignette**). Full Comet Club pitch (act beats, evidence progression, suspect web, red-herring lane, 3 endings, suggested `meta.json` synopsis + `facultyDistribution`). Mythos thread staged: 4 recurring motifs, 6 breadcrumb flags, keystone `KeyDeduction` `mythos-pattern-named` (Orrery Room only), main cases faint-echo-only with one deliberate no-Mythos control case (A Season of Masks) — all within existing flag/recipe/variant machinery. Ideation only per the prompt — no JSON authored.
  - **Doc-drift sweep fixes:** root `README.md` had stale claims that survived the PR #69 sweep — play URL `jonwhitefang.github.io` → `holodeck.jonwhitefang.uk/gaslight-and-grimoire/`, "198 scenes" → 201, "React 18" → 19 (verified `package.json` `react ^19.2.7`), "Tailwind CSS" → v4, and the CI section's "deploys to GitHub Pages" → build-compiles check / Cloudflare-side deploy (verified `wrangler.jsonc` exists). `docs/README.md` doc map gained rows for `content-ideas-prompt.md` + the new ideas doc.
- **Verified:** n/a — no code/content changed; `git status` shows docs only. **Could not run** the validator or test suite this session: `node_modules` is absent locally (`validateCase.mjs` fails with vite-node ENOENT). Content JSON is untouched since the 2026-07-09 verified baseline, so **611 tests / 58 files / 201 scenes / 7 cases** stand from that session's live run.
- **Open / blockers:** `node_modules` missing locally — run `npm ci` before the next code session. The 10 concepts await a user pick (suggested build order in the doc: Comet Club → Orrery Room → Drowned Archive → Tidewaiter's Log). #20 (media) unchanged; #52 answered, closable by the user.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no architectural decision — ideation output plus verified drift fixes; nothing chosen with lasting structural consequences until a concept is green-lit).

## 2026-07-09 — Answered the issue #52 agent-perspective interview (posted to GitHub, not the repo)

- **Goal:** The user asked the agent to answer **issue #52** — an interview addressed to Claude Code for a jonwhitefang.uk blog post about how Gaslight & Grimoire was built ("give me *your* side, candour over polish"). Then checkpoint + commit/push/merge the spine update.
- **Did (no code/content touched — the answer was posted to GitHub, not committed):**
  - **Research:** ran a fan-out `Workflow` — 8 per-cluster researchers (one per interview question-group), each grounding every claim in a git hash / PR / ADR / RUN_LOG citation, then a fact-check phase. The fact-check batches largely **stalled on agent timeouts** (11 of 29 agents errored) and returned **zero corrections**; the 8 research clusters all completed. Because the fact-check layer was unreliable, I **independently re-verified every load-bearing figure** against primary sources before writing: `npm run test:run` (611), `node scripts/validateCase.mjs` (201 scenes / 7 cases), `git show be92632` (initial commit contains `.kiro/`), `4707828` (Claude-Code migration), `grep` for engine store-imports (only `__tests__/` import the store), and PR #51's body (deps).
  - **Posted** a first-person, candour-over-polish answer to all 11 questions as a comment on issue #52 → [comment 4926364394](https://github.com/JonWhiteFang/gaslight-and-grimoire/issues/52#issuecomment-4926364394). Central honesty points surfaced (so the blog doesn't overclaim): (1) the project **began under Kiro** — the design bible, 4 archetypes, 4 factions, deduction system, the whole Phase A–E foundation and the "198 scenes" uplift (`50df7a1`) all predate Claude Code's 2026-07-07 takeover (`4707828`); (2) "198 scenes" is a stale Kiro-era commit-title number — live is **201 base / 215 runtime** (F-120); (3) there were **two** audits (67→22 + 23→8), not one — and audit-1 missed a P0 (F-101); (4) the deduction mechanic was **activated** (stable ids + authored content), not built; (5) the human/agent division of labour is **invisible in git** (every non-bot commit is "Jon White"); (6) PR #51 shipped the React-19 `inert` regression green (caught only by audit-2); (7) the memory spine was retrofitted 2026-07-07 and its main cost is the recurring drift sweep.
- **Verified:** `npm run test:run` → **611 passed (611)** / 58 files; `node scripts/validateCase.mjs` → **7 cases clean, 201 scenes**. Working tree was clean at session start and after posting (the interview is a GitHub comment, not a repo change). Doc-drift sweep: ground truth (611/58, 201/7) matches CLAUDE.md/status.md — no drift there; the only correction was PROJECT_STATE's prior `_Last updated:_` still calling the doc-sweep "uncommitted" when PR #69 had merged it → fixed while demoting it to `_Prior update:_`.
- **Open / blockers:** #52 answered (blog write-up + closing the issue are the user's calls). Only open code-adjacent issue remains #20 (media, user-blocked). No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no architectural decision — answering an interview + a spine update; no choice with alternatives/consequences to record).

## 2026-07-09 — Full doc-drift sweep (21 fixes) + relocated the 2026-07-09 audit report to docs/audits/

- **Goal:** Full sweep of every project doc for drift against current code/content (a deeper pass than the per-session `/checkpoint` sweep), then — at the user's request — move the second Ultracode audit report out of the repo root into `docs/audits/` beside the first.
- **Did (docs-only, uncommitted — no `src/`/config/`public/content/` touched):**
  - **Doc-drift audit via Workflow:** fan-out of 9 per-doc auditors (CLAUDE.md + the 8 live `docs/`) + a historical-doc link-check, each finding **adversarially verified against source** before acceptance. 21 CONFIRMED fixes applied across **8 files**; PROJECT_STATE + all historical docs (RUN_LOG, ADRs, both audit reports, design bible, plans/specs) came back clean (0 broken links, history left frozen). Fixes: **engine-reference.md** — `CaseCompletionResult.vignetteUnlocked:string|null` → `vignettesUnlocked:string[]` and `checkVignetteUnlocks():string|null` → `:string[]` (both stale since the F-057 plural-unlock change), `evaluateCondition` "(exported)" → module-internal, audioManager "no assets ship" → 9 SFX ship. **CLAUDE.md** — scene-count parenthetical (201 = base+variants **excl.** shared; 215 at runtime), added `haltScenes.ts` to the engine list, added `CaseCompletion` + `InvestigationHalted` to the component hierarchy (the latter as a GameContent body-swap, not a top-level screen), content JSON described as wrapped objects not bare arrays, the self-contradictory "No audio files in repo" warning corrected. **content-authoring.md** — audio-not-present blurb (9 SFX now ship), `66→67` validator example, dead `build→deploy` CI dependency (there is no deploy job). **architecture.md** — 2nd `useStore` test file added to the verification block, two stale `file:line`s (`diceEngine.ts:23→24`, `metaSlice.ts:43→56`; verified the other five still accurate). **README.md** doc-map missing `audio-asset-kit.md` + build-command comment; **status.md** header date `07-07→07-09`; **audio-asset-kit.md** + **DECISIONS/README.md** (ADR-0006) audio-status lines → "9 SFX shipped, ambient pending / partially enacted".
  - **Audit-file move:** `git mv 2026-07-09_ULTRACODE_FULL_REPO_ANALYSIS.md docs/audits/` (history preserved; file is self-contained, no internal links to break). Repointed the **4 live** references in `PROJECT_STATE.md` (`../` → `audits/`, "repo root" prose → `docs/audits/`). `RUN_LOG.md` history: the entry below linked it at the old root path — **not rewritten**; per the ADR-annotation rule the *href* was repointed and a dated "(moved to docs/audits/ on 2026-07-09 — link repointed)" note added, preserving the "repo root at the time" fact. The other RUN_LOG mention (checkpoint-of-#58 entry) is prose-only (no href) and left as-is.
- **Verified:** `node scripts/validateCase.mjs` → 7 cases clean. **All 85 internal `.md` links resolve (0 broken)** post-move (custom link-walker over `docs/` + `CLAUDE.md` + `README.md`). `git diff --name-only` = docs only (+ the tracked rename), so the **611/58** test baseline is unaffected (no code touched — suite not re-run for that reason). Every stale string confirmed removed and its replacement present via grep.
- **Open / blockers:** None. Doc set now matches code. Changes are **uncommitted** — surfaced for user review. (Audit-2 code backlog remains CLEAR; only #20 media + #52 interview open, both unchanged.)
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no architectural decision — a doc-accuracy sweep + a file relocation; the "keep audit reports under docs/audits/" convention already existed, this just conforms the newer report to it).

## 2026-07-09 — Audit-2 #60 (P3): CI type-checks scripts/ + vite.config.ts (PR #68) — backlog CLEAR

- **Goal:** Fix the last audit-2 code item (#60, F-123): the build gate's `tsc` (`tsconfig.json` `include:["src"]`) never type-checked `scripts/` — incl. `scripts/validateCase.ts`, the content-validator source that is itself a CI correctness gate — nor properly `vite.config.ts` (its `tsconfig.node.json` reference had no `lib`, so `tsc -b` reported `TS2550` on `String.includes`).
- **Did (branch `fix/60-typecheck-scripts`, PR #68 **merge-committed** to `main` as `a5d4265` per the no-squash rule, branch deleted):** RED-first — injected `const x: string = 42` into the validator source and confirmed `npm run build` exited **0** (the bug), then implemented and confirmed it exits **2** (also verified for a type error in `vite.config.ts`). New **`tsconfig.scripts.json`**: non-composite + `noEmit` (so, unlike the `composite` `tsconfig.node.json`, it avoids `TS6310` and emits no `vite.config.js`/`.d.ts` litter — the litter PR #61 had to gitignore), `target ES2022`/`lib ES2023`, `types:["node"]`, `include: ["scripts/**/*.ts","vite.config.ts"]`. New `typecheck:scripts` npm script (`tsc -p tsconfig.scripts.json`) chained into `build` between `tsc` and `vite build` (fails fast). `tsconfig.node.json` given explicit `target ES2022`/`lib ES2023` to fix the `TS2550` at root (rather than leaning on `@types/node`'s implicit `/// <reference lib="es2020" />`). Added `@types/node@^20` devDep, pinned to the `.nvmrc` Node-20 line (scripts use `process`/`fs`/`path`). `deploy.yml` build-job comment documents the expanded `tsc` scope; `CLAUDE.md` Commands block + CI/CD `build` description updated. **No `src/` or content changes.**
- **CI round-trip (one failure, fixed):** first push's `test` job failed at `npm ci` — "Missing @emnapi/core@1.11.2 from lock file" — the **emnapi lockfile trap** ([[dependabot-major-group-migration]]). Root cause: local Node **v25 / npm 11.6.2** vs `.nvmrc`+CI **Node 20.19**; the newer npm re-resolved the `^1.11.1` optional `@emnapi/*` ranges to 1.11.2, and an incremental `npm install @types/node` (even with a converge-loop) left the lock inconsistent. Fixed per the memory note: `rm -rf node_modules package-lock.json && npm install` (resolved emnapi back to 1.11.1, matching main) → `npm ci --ignore-scripts` verify (exit 0) → `git commit --amend` + `--force-with-lease`. Updated [[dependabot-major-group-migration]] with the Node-version compounding factor + "never incrementally-install into this lock" takeaway.
- **Verified:** `npm run test:run` → **611 passed (611)**, 58 files (unchanged — no `src/` touched). `npm run lint` clean, `node scripts/validateCase.mjs` → 7 cases clean, `npm ci --ignore-scripts` + `npm run build` green (no emit litter). RED confirmed (build exit 0 → 2 on a validator type error). **All six CI checks green on PR #68** (incl. `build` job with the new gate + `test` job whose `npm ci` now passes). Issue #60 auto-closed on merge. Re-verified on merged `main`: `npm ci` + build + validator all green.
- **Doc-drift sweep (auto-fixed this session's drift):** `CLAUDE.md` Commands block (build tsc scope + new `typecheck:scripts`) + CI/CD `build`-job description (scripts type-check note). `PROJECT_STATE.md` header/Current-position/Active-gate/Branch-focus (`a5d4265`)/Verification/Next-actions/Open-questions/R3′-row — all now show the audit-2 backlog CLEAR. No test-count change (611). No broken internal links; no `public/content/` changes (content-integrity gate skipped).
- **Open / blockers:** **Audit-2 code backlog fully cleared (#53–#60).** Both audit backlogs now done except #20 (media, user-blocked). Only open issues: #20 + #52 (interview, non-code). No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new architectural decision — a CI-gate hardening within the existing build/tsconfig structure; the `noEmit` non-composite choice is the standard TS pattern for a check-only project). Also updated the [[dependabot-major-group-migration]] auto-memory with the re-confirmed emnapi trap.

## 2026-07-09 — Audit-2 #59 (P2): test quality — real-unit, mutation-verified guards (PR #67)

- **Goal:** Fix the P2 test-quality backlog (#59, F-112–F-116 + 2 correctness cross-check gaps): the 588-test suite was green while several invariants had *zero real guards* because the tests exercised copies of logic, transitive paths, or asserted nothing about the behaviour they were named for.
- **Did (branch `fix/59-test-quality`, PR #67 **merge-committed** to `main` as `629df31` per the no-squash rule, branch deleted):** **test-only** — no production code changed. Because the product code was already correct, the honest RED was **mutation verification**: for each new/rewritten test I broke the real production code, watched the test fail, then restored it. **F-112** rewrote `npcBounds.property.test.ts` to drive the real `adjustDisposition`/`adjustSuspicion` slice actions and assert the *store* clamps (was asserting local `clampDisposition`/`clampSuspicion` copies) — verified removing either `Math.max`/`Math.min` now fails. **F-113** new `worldSlice.reputation.test.ts` — real-slice ±10 reputation boundary tests (was only `vi.fn()`-mocked in engine tests). **F-114** added Nerve-vs-Lore reaction-faculty tiebreak tests to `encounterSystem.test.ts` (tie → Nerve, plus both strict-inequality cases); flipping `>=`→`>` fails only the tie. **F-115** new `resolveScene.test.ts` — direct variant-resolution tests (variant-when-met / base-when-not / no-variant / wrong-target / multi-select / conditionless-ignored / missing-scene throw); was only exercised transitively (all variant tests used `variants: []`). **F-116** rewrote the mislabeled `metaSlice.saveGame` eviction test with deterministic timestamps to assert the *oldest* manual save is evicted and the autosave slot is protected (was asserting a list length only); evict-newest and drop-the-`!=='autosave'`-filter mutations now fail. **flag path** added `applyEffects` result assertions (flag `false`/`true`/string + composure/vitality/reputation) to `worldSlice.effects.test.ts` (was "does not throw"); the `?? true`→`|| true` mutation that coerces `false`→`true` now fails. **mundane vitality-only** added the encounter single-axis vitality-only + both-deltas exclusivity cases (was only composure-only covered); deleting the `else if (vitalityDelta …)` branch now fails.
- **Verified:** `npm run test:run` → **611 passed (611)**, **58** files (was 588/56; +23 tests). `npm run lint` clean, `node scripts/validateCase.mjs` → 7 cases clean, `npm run build` green (incl. Cloudflare nest postbuild). Each mutation confirmed to fail the intended test(s) before restore; `git diff` of `main` vs branch on non-test `src/**` is **empty**. **All six CI checks green on PR #67.** Issue #59 auto-closed on merge.
- **Doc-drift sweep (auto-fixed this session's drift):** `status.md` test baseline `588`→`611`, files `56`→`58`. `PROJECT_STATE.md` header/Current-position/Verification/Branch-focus updated; R3 row split (P2 #59 `[x]` PR #67, new R3′ for the remaining P3 #60); Next-actions + Open-questions now show only #60 outstanding; `main` at `629df31`. No broken internal links; no `public/content/` changes (content-integrity gate skipped). Noted the prior #57 entry records PR #66 as "squash-merged" — left as a point-in-time record (not rewritten); the no-squash rule postdates it (added in `e1ffe07`).
- **Open / blockers:** All audit-2 P0/P1/P2 done. Remaining code item: **#60 (P3)** — CI build gate `tsc` doesn't type-check `scripts/` incl. the validator source (F-123); its `tsc -b` also surfaces pre-existing `TS2550` on `vite.config.ts` that must be handled. Media track (#20) unchanged — user-blocked. No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new architectural decision — hardening within the existing test strategy; the mutation-verify discipline is method, not a design choice).

## 2026-07-09 — Audit-2 #57 (P1): a11y & error-messaging (PR #66)

- **Goal:** Fix the last remaining P1 — the three a11y/error-messaging defects in #57 (React-19 `inert` regression, false auto-save claim, unannounced loading).
- **Did (branch `fix/57-a11y-error-messaging`, PR #66 squash-merged to `main` as `08335e6`, branch deleted):** TDD (RED watched for each). **inert (F-007 regression)** — `App` set the background inert via the React-18 idiom `inert: '' as unknown as boolean`; verified against `node_modules/react-dom/cjs/react-dom-client.development.js:20218` that React 19's `inert` case falls through to the boolean-attribute handler and `removeAttribute`s a falsy empty string, so opening an overlay *stripped* `inert` instead of setting it (3 of 4 overlays have no independent Tab trap and relied on it). Fixed to `inert={anyOverlayOpen}`. **ErrorBoundary** — "your progress has been auto-saved" was unconditional but Manual save mode writes no autosave slot; gated on `SaveManager.listSaves().some(s => s.id === 'autosave')`, else warns progress may be lost (imports `SaveManager`). **Loading** — added `role="status"` + `aria-live="polite"` to `OverlayFallback` (now exported for testing) + the "Loading case…" screen. **Bonus (determinism):** while verifying I hit a **pre-existing flaky property test** (`narrativeEngine.property.test.ts` "structurally equal states", fails ~1/7 full-suite runs *on `main`*). Root cause: record-indexed gates read by bracket index, so a target naming an `Object.prototype` member (`toString`/`valueOf`/`constructor`) resolved to the inherited property — a `hasFlag:{target:'toString',value:true}` gate passed on any plain-object state and the result flipped on null-proto maps (fast-check generates them). Added an own-property guard `ownValue` in `conditions.ts` applied to all record lookups (`hasClue`/`hasDeduction`/`hasFlag`/`npcDisposition`/`npcSuspicion`/`factionReputation`/`npcMemoryFlag`); `facultyMin` left as a direct index (fixed-key typed record). User approved fixing the product code (vs test-only). **Tests:** App (background carries `inert` when overlay open, not when closed; `OverlayFallback` + loading screen are `role=status`/polite), ErrorBoundary (auto-save claim present only when an `autosave` slot exists), conditions (inherited-key gate never satisfied across `hasFlag`/`hasClue`/`hasDeduction`/`npc*`; plain-`{}` vs null-proto determinism).
- **Verified:** `npm run test:run` → **588 passed (588)**, 56 files (was 577; +11). `npm run lint` clean, `node scripts/validateCase.mjs` → 7 cases clean, `npm run build` green. RED confirmed for each change before GREEN. Property suite **20/20** clean post-fix (was ~1/7 failing). **Real-browser (Playwright):** drove new-game → game screen → opened Evidence Board → background carries `inert=""` and focusing a background header button fails (focus stays on "Close Evidence Board" inside the dialog). **All six CI checks green on PR #66.** Issue #57 auto-closed on merge.
- **Doc-drift sweep (auto-fixed this session's drift):** `status.md` test baseline `577`→`588`. `PROJECT_STATE.md` header/Current-position/Verification/Branch-focus/R2-row/Next-actions updated (all P1 now `[x]`; `main` at `08335e6`). No broken internal links; no `public/content/` changes (content-integrity gate skipped).
- **Open / blockers:** All P0 + P1 of audit-2 done. Remaining: **#59 (P2)** test-quality (F-112–F-116) and **#60 (P3)** CI type-check `scripts/` (F-123). Media track (#20) unchanged — user-blocked. No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new architectural decision — the `inert` fix restores intended F-007 behaviour; the `ownValue` guard is a local hardening within the existing pure-condition-eval design).

---

## 2026-07-09 — Audit-2 #55 (P1): save/reload safety (PR #65)

- **Goal:** Fix the two P1 persistence-safety defects — silent manual-save failure (F-103) and encounter reaction-check re-roll on reload (F-105).
- **Did (branch `fix/55-save-reload-safety`, PR #65 squash-merged to `main` as `9b69046`, branch deleted):** TDD (RED watched). **F-103** — `saveGame` had no try/catch though `autoSave` did; a `localStorage.setItem` throw (quota / private browsing / disabled) became an unhandled rejection while `handleSaveGame` showed "Game saved". Wrapped the save in try/catch; `SaveResult` gained an `ok` flag; `App` shows an error toast (`role="alert"`, assertive, red styling) on `ok:false`. **F-105** — `EncounterState` (`currentRound`, `reactionCheckPassed`) was component-only + unpersisted, so an autosave-on-encounter-entry + reload re-ran `startEncounter` → re-rolled the reaction check, re-applied composure damage (save-scum), reset the round. Persisted `encounterState` in `narrativeSlice` (+ `setEncounterState` action) + `snapshotGameState` + `loadGame`; new save **v3→v4** migration defaults it to null; `EncounterPanel` resumes a persisted in-progress encounter for the scene (skips `startEncounter`) and mirrors round progress back to the store; `goToScene` clears it on cross-scene nav (reusing the F-106 guard) and `resetForNewCase` clears it. **Tests:** metaSlice.saveGame (`ok:false` on throw / `ok:true` normal), App (real new-game flow → game screen → `SaveManager.save` throws → `role="alert"` error toast, not "Game saved"), metaSlice.loadGame (encounterState round-trips), EncounterPanel (resumes persisted / runs when none), saveMigration (v3→v4 default null + untouched-when-present, v0→4 chain).
- **Verified:** `npm run test:run` → **577 passed (577)**, 56 files (was 569; +8). `npm run lint` clean, `node scripts/validateCase.mjs` → 7 cases clean, `npm run build` green. RED confirmed failing for the right reasons before each GREEN. **All six CI checks green on PR #65.**
- **Doc-drift sweep (auto-fixed this session's drift):** `status.md` baseline `569`→`577`. Save version `3`→`4` + the v3→v4 migration step in `CLAUDE.md` (Save System) + `engine-reference.md` (`CURRENT_SAVE_VERSION` + `migrate`). Added `encounterState`/`setEncounterState` to the narrativeSlice rows in `CLAUDE.md` + `architecture.md`. Noted the `saveGame` try/catch + `autoSave` encounter-state snapshot in `CLAUDE.md`. No broken internal links.
- **Open / blockers:** #55 done. **#57 is the last remaining P1** (a11y/errors — React-19 `inert` regression, false auto-save claim, unannounced loading; spans several components). Then P2 #59, P3 #60. No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new architectural decision — persisting `encounterState` extends the existing save/snapshot pattern + migration pipeline; the try/catch mirrors `autoSave`).

## 2026-07-09 — Audit-2 #56 (P1): scene-transition state hygiene (PR #64)

- **Goal:** Fix the P1 root-cause cluster on the `goToScene`/`resetForNewCase` seam — four related defects (F-118/F-104/F-106/F-108), one PR since they touch the same two functions.
- **Did (branch `fix/56-scene-transition-hygiene`, PR #64 squash-merged to `main` as `469a86a`, branch deleted):** TDD (RED watched, 5 tests). **F-118** — the once-per-scene `onEnter` gate keyed on the *base* `sceneId` then resolved a scene that might be a *variant* with a different `onEnter`; a hub entered base-first then re-entered after its variant condition flipped never fired the variant's `onEnter`. Fixed by resolving first, then gating + recording `visitedScenes` on the **resolved** scene id (base or variant). Non-variant path unchanged (`resolvedId === sceneId`); F-006 idempotency preserved. **F-104** — `resetForNewCase` never reset `currentScene`, so the new case's first `goToScene` pushed the stale foreign id into the fresh `sceneHistory`; added `state.currentScene = ''`. **F-106** — `goToScene` never cleared `lastCheckResult`, so the dice overlay floated onto the destination scene; now cleared on a real cross-scene nav (`prevScene !== sceneId`), left in place on same-scene re-nav; not persisted so saves unaffected. **F-108** — `App` read `sceneHistory` non-reactively for `canGoBack`; replaced with a reactive `useStore((s) => s.sceneHistory.length > 0)` selector. **Tests:** goToSceneEffects (variant `onEnter` fires once when newly eligible + no re-fire; `lastCheckResult` cleared on nav), narrativeSlice (loadAndStartCase → empty `sceneHistory`), and an **App integration test** driving the real new-game flow (archetype radio → allocate 12 → begin → select case via mocked fetch) to the game screen, then asserting the review button enables reactively after `goToScene`.
- **Verified:** `npm run test:run` → **569 passed (569)**, 56 files (was 564; +5). `npm run lint` clean, `node scripts/validateCase.mjs` → 7 cases clean, `npm run build` green. RED confirmed failing for the right reasons before each GREEN. **All six CI checks green on PR #64.**
- **Doc-drift sweep (auto-fixed this session's drift):** `status.md` baseline `564`→`569`. The `onEnter`/`visitedScenes` behaviour notes in `CLAUDE.md` (×2) + `architecture.md` (×2, incl. the data-flow diagram) now say the gate keys on the **resolved** scene id (F-118) and note the F-104 `currentScene` reset + F-106 `lastCheckResult` clear. Also fixed drift left over from **#54**: `engine-reference.md`'s `validateBundle` entry now documents the F-102 gated-deduction reachability error + added a `computeNonCriticalReachableScenes` line. No broken internal links.
- **Open / blockers:** #56 done. Remaining P1: #55 (save/reload safety — suggested next, self-contained) + #57 (a11y/errors, spans components). Then P2 #59, P3 #60. No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new architectural decision — the resolved-id gate refines the existing once-per-playthrough `onEnter` invariant, ADR-0001; no new direction).

## 2026-07-09 — Audit-2 #54 (P0): Mayfair true ending reachable through skilled play + validator guard (PR #63)

- **Goal:** Fix the last audit-2 P0 gameplay blocker — the Mayfair true ending gated behind a key deduction whose clues needed two nat-20 rolls (F-102) — and add the per-tier reachability validator the audit flagged as the mechanical follow-up.
- **Did (branch `fix/54-mayfair-true-ending`, PR #63 squash-merged to `main` as `6b96b28`, branch deleted):** TDD (RED watched). **Verified the bug first** with a JSON + path analysis across all 3 deduction-bearing cases: only Mayfair is crit-gated — `ms-clue-vesper-journal` (sole source `ms-act2-occult-success`, occult-path crit) and, on an occult run, `ms-clue-hidden-mechanism` (only `ms-act1-room-thorough`, room-exam crit; its other sources are on the mutually-exclusive rational branch). **Validator guard (RED-first):** new per-tier rule in `contentValidation.ts` — for every clue required by a *gated* key deduction (referenced by `requiresDeduction`/`hasDeduction`/`ClueDiscovery.requiresDeduction`), at least one OBTAINABLE source (`cluesAvailable` or `onEnter discoverClue` — deliberately NOT a mere `requiresClue`/`advantageIf` reference, which was the trap that first hid the bug) must be reachable on the non-critical scene graph. Build-time only (opt-in `includeReachability`), so runtime `validateContent` is unaffected. Added `computeNonCriticalReachableScenes` / `computeObtainableClues` / `collectGatedDeductionIds` + `nonCriticalOutgoingEdges`. Errored on Mayfair before the content fix. **Content fix:** dual-sourced `ms-clue-vesper-journal` onto `ms-act2-occult-partial` (exploration; journal slips from Vesper's coat as she collapses — coherent against the occult-success beat where she presses it into your hands) and `ms-clue-hidden-mechanism` onto `ms-act1-room-partial` (`check`, perception≥11, matching the case's `ms-clue-servant-key` convention). 6 new validator unit tests.
- **Verified:** simulated a skilled no-crit occult run (perception≥11) → gathers all 3 required clues → gate choice `ms-choice-full-account` routes to `ms-act3-ending-true-account` on success/partial (deduction check itself is the retryable DC-14 Reason check, not a crit). `npm run test:run` → **564 passed (564)**, 56 files (was 558; +6). `npm run lint` clean, `node scripts/validateCase.mjs` → 7 cases clean (new rule passes on all), `npm run build` green. **content-integrity-reviewer** dispatched on the narrative edits → **clean** (no blockers/warnings; independently traced the crit-free path; flagged one tone nit, applied). **All six CI checks green on PR #63.**
- **Doc-drift sweep (auto-fixed this session's drift):** `status.md` test baseline `558`→`564` + added the new F-102 rule to the validator-check list; `content-authoring.md` key-deduction authoring rule now notes the gated-clue-must-be-non-crit-obtainable check is validator-enforced. No broken internal links.
- **Open / blockers:** **Both P0s now done.** Next: P1 #56 (scene-transition state hygiene, groups 4 defects on the `goToScene`/`resetForNewCase` surface → one PR), then #55 (save/reload safety) + #57 (a11y/errors). Then P2 #59, P3 #60. No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new architectural decision — the content fix applies the existing content-authoring rules, ADR-0001/ADR-0005; the validator rule extends the existing reachability analysis, not a new direction).

## 2026-07-09 — Audit-2 #53 (P0): auto-succeed ability consumed once + check pipelines unified (PR #62)

- **Goal:** Fix the highest-value audit-2 P0 gameplay blocker — the archetype once-per-case auto-succeed ability that was never consumed (F-101) and its mirror dead-path inside encounters (F-107).
- **Did (branch `fix/53-ability-auto-succeed`, PR #62 open — all 6 CI checks green, awaiting merge):** TDD throughout (RED watched before each fix). **Root cause:** `handleActivateAbility` set the persistent world flag `ability-auto-succeed-{faculty}`; `computeChoiceResult` read it to force tier `critical` but **nothing cleared it mid-case** (only the next case-load's `resetForNewCase` did), so after one activation every same-faculty choice-check auto-crit for the rest of the case — while the greyed-out button implied a spent one-shot. Meanwhile `processEncounterChoice` ran a **parallel** check pipeline that never read the flag (and ignored `dynamicDifficulty`) → same ability dead in encounters (F-107). **Fix:** extracted a single pure `resolveCheckOutcome(choice, state, label)` in `choiceResolution.ts` returning `{ result, consumedAbilityFlag? }`; `computeChoiceResult` is now a thin wrapper over it (unchanged pure signature — no caller ripple). Both `processChoice` **and** `processEncounterChoice` now consume the ability on use via the existing `actions.setFlag(flag, false)` seam (no new `clearFlag` needed) → fires exactly once, in choices AND encounters; encounters also gain dynamic-difficulty DCs + advantage for free (no existing content uses encounter `dynamicDifficulty`, verified — that part is defensive-only). **Tests:** added F-101 consumption tests (`integration.test.ts`) + F-107 auto-succeed/dynamicDifficulty tests (`encounterSystem.test.ts`); **replaced** the two self-fulfilling `setState`-simulation guard tests in `AbilityButton.test.tsx` with ones that drive the real `resetForNewCase` (this also pre-empts part of #59).
- **Verified:** `npm run test:run` → **558 passed (558)**, 56 files (was 554; +4 net). `npm run lint` clean, `node scripts/validateCase.mjs` → 7 cases clean, `npm run build` green. `git grep` confirmed no content references the ability flags (so the flag stays engine-internal). **All six CI checks pass on PR #62** (test, build, npm audit, OWASP Dependency-Check, Cloudflare Workers build). RED confirmed failing for the right reasons before each GREEN.
- **Doc-drift sweep (auto-fixed this session's drift):** `status.md` test baseline `554`→`558`. `CLAUDE.md` + `docs/engine-reference.md` — rewrote the `processChoice`/`computeChoiceResult`/`processEncounterChoice` behaviour entries to add the new `resolveCheckOutcome` shared unit and the flag-consumption (F-101/F-107); prior text implied the ability was set-and-not-consumed. The engine-reference intro callout stayed accurate (no edit). No broken internal links.
- **Open / blockers:** #53 done pending PR #62 merge. **Next: #54** (Mayfair true ending RNG-locked; F-102) — last P0. Then P1 #55/#56/#57, P2/P3 #59/#60. No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new architectural decision — the shared-unit extraction implements the existing engine↔store seam / single-source-of-truth patterns, ADR-0001; no alternatives with lasting consequences to record).

## 2026-07-09 — Audit-2 #58: doc-drift + `.gitignore` gap fixed (PR #61)

- **Goal:** Clear the two follow-ups left after the audit checkpoint — the doc-drift the audit *found* (F-119–F-122, tracked in #58) and the `.gitignore` gap for the `vite.config` build litter.
- **Did (PR #61, `3dc49aa`, squash-merged to `main`, branch deleted):** Branch `chore/audit2-docs-drift-and-gitignore`. **F-119** — `docs/architecture.md` said `applyEffects` is "invoked from `NarrativePanel` on scene entry" (the exact F-006 anti-pattern `CLAUDE.md` forbids) → corrected to `narrativeSlice.goToScene`, gated on `visitedScenes` (once per playthrough), view-layer read-only; **also** fixed the adjacent data-flow note where the removed `last-critical-faculty` flag should be the typed `investigator.lastCriticalFaculty` field (F-013). **F-120** — scene counts `66/49/43`→`67/50/44`, total `198`→`201` in `status.md` + `CLAUDE.md` (matching the validator both cite). **F-121** — component count `16`→`17`, added `InvestigationHalted` to the tree, count, and list. **F-122** — "2.0+ choices/scene" → ~1.77 across all content (2.14 among choice-bearing scenes). **`.gitignore`** — added `vite.config.js`/`.d.ts`.
- **One course-correction worth remembering:** first tried fixing the emit at source with `noEmit:true` in `tsconfig.node.json` — it **broke `npm run build`** (`TS6310: referenced composite project may not disable emit`). Caught by verifying the real build path end-to-end (not just the isolated `tsc -b`). Reverted; used gitignore instead. The plain `tsc` in `npm run build` never emits these — only `tsc -b` (IDE/build-mode) does, from the composite reference.
- **Verified:** `npm run lint` clean, validator 7 cases clean, `npm run build` green, `npm run test:run` → **554 passed (554)**. All 6 CI checks green on PR #61 (test, build, npm audit, OWASP Dependency-Check, Cloudflare Workers build). All figures verified against ground truth (validator + a content scan), not guessed.
- **Doc-drift sweep (this checkpoint):** updated the spine to reflect #58 done — `_Last updated:_`, tracker (split R2 → R2 code-P1 `[ ]` + R2′ docs-P1 `[x]`), Next actions (removed #58 from the P1 list), and marked the `.gitignore` Open question RESOLVED. **Also found + fixed a pre-existing broken link:** `audio-asset-kit.md` lives at `docs/audio-asset-kit.md`, but PROJECT_STATE.md (×3) and status.md (×1) linked it as `../audio-asset-kit.md` (dead — resolves to repo root). Fixed to `audio-asset-kit.md`; ADR-0006's `../` ref is correct from `docs/DECISIONS/` and left alone. No code touched → 554 baseline holds. The audit *report* itself (`2026-07-09_ULTRACODE_FULL_REPO_ANALYSIS.md`) is left as a point-in-time deliverable — not retro-edited to mark F-119–F-122 fixed.
- **Open / blockers:** **Audit-2 code backlog remains: #53/#54 (P0), #55/#56/#57 (P1 code), #59/#60 (P2/P3).** Next up is #53 (auto-succeed ability — highest-value P0, sets up the shared check-helper for #56/F-107). F-123 note: #60's `tsc -b` surfaces pre-existing `TS2550` errors on `vite.config.ts`.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no non-trivial decision — the noEmit-vs-gitignore call is recorded here + in PROJECT_STATE, too small for an ADR).

---

## 2026-07-09 — Second full Ultracode repo audit (analysis only) → 23 findings, 8 issues #53–#60

- **Goal:** Run a fresh, reachability-aware full-repo audit (the `/effort ultracode` super-prompt) hunting *new* subtle defects that survived the first audit (F-001…F-067, all closed), then file grouped GitHub issues. **Analysis only — no code changes.**
- **Did (evidence):** Ran the command battery — `npm run lint` clean, `node scripts/validateCase.mjs` → 7 cases clean (reports **67/50/44** scenes for the main cases), `npm run build` green, `npm run test:run` → **554 passed (554)** / 56 files, `npm audit` → **0 vulnerabilities**, `npm outdated` → only TS (held). Secrets grep clean.
- **Did (audit):** Orchestrated a 13-dimension adversarial fan-out via the Workflow tool (specialist finder per dimension → 2 independent skeptics/finding, diverse lenses → dedup/synthesis). The background workflow got orphaned twice when the host process yielded between turns (lifecycle quirk, not logic failure); completed on the third run — **71 agents**. Cross-checked with 4 synchronous verification subagents + direct lead re-verification of every High/Medium finding against source. **37 raw → 25 verified survivors → 23 root-cause-deduped**; **4 rejected** as false positives. Overall risk **Medium**. Security pass found nothing (locked CSP, no `innerHTML`/`eval`/secrets). Wrote [`2026-07-09_ULTRACODE_FULL_REPO_ANALYSIS.md`](audits/2026-07-09_ULTRACODE_FULL_REPO_ANALYSIS.md) (repo root at the time; moved to `docs/audits/` on 2026-07-09 — link repointed). New IDs **F-101…F-123** (avoid collision with F-001…F-067).
- **Did (issues):** Filed **8 grouped issues #53–#60** matching the repo's `[Px] <theme> (F-###)` convention + priority/category labels. **P0:** #53 archetype auto-succeed ability never consumed — auto-crits every same-faculty check for the whole case (F-101 + its mirror F-107, dead in encounters); #54 Mayfair true ending RNG-locked behind two nat-20s (F-102). **P1:** #55 save/reload safety (F-103 silent save fail, F-105 encounter reaction re-roll); #56 scene-transition state hygiene (F-118 variant onEnter drop, F-104 currentScene leak, F-106 overlay leak, F-108 stale back button); #57 a11y+errors (React-19 `inert=''` regression defeating F-007, false auto-save claim, unannounced loading); #58 docs-drift (F-119 `architecture.md:101` documents the F-006 anti-pattern; F-120–F-122 stale counts). **P2:** #59 test-quality (F-112–F-116 — copy-of-logic + untested clamps/branches). **P3:** #60 CI type-check `scripts/` (F-123). One transient network error mid-creation; verified #53/#54 landed and retried — no dupes/gaps.
- **Verified:** Command battery above (all green). Every High/Medium finding lead-verified against source or `node_modules/react-dom` internals (the React-19 `inert` behaviour). No code touched → test baseline **554 unchanged**.
- **Doc-drift sweep:** The status drift this session *introduced* — PROJECT_STATE's "entire P0–P3 audit backlog is cleared" — is now false; **fixed** (reopened backlog, added Audit-2 tracker rows R/R2/R3, rewrote Next actions, updated Open questions + References + `_Last updated:_`). The drift the audit *found* (F-119 `architecture.md` onEnter anti-pattern; F-120 scene counts 66/49/43→67/50/44 & 198→201; F-121 16→17 components; F-122 choices-per-scene ~1.77 not 2.0+) is **not auto-fixed here** — it's now tracked in issue #58 with a fix plan (F-119 is a judgment-call invariant rewrite, outside the mechanical-sweep scope), so it belongs in that PR. **Flagged, not fixed.**
- **Housekeeping:** Removed two stray build artifacts (`vite.config.js`, `vite.config.d.ts`) that `tsconfig.node.json`'s `composite:true` emitted during the audit build and that `.gitignore` doesn't cover. The `.gitignore` gap is flagged in PROJECT_STATE Open questions for a small chore PR.
- **Open / blockers:** **Audit-2 backlog #53–#60 is verified but unfixed** — code work starts at #53. First-audit backlog still clear except #20 (media, user-blocked). Watch: the `.gitignore` gap re-surfaces the vite.config artifacts after every local build until fixed.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no non-trivial *decision* — analysis + triage, not an architectural choice) · auto-memory ☑ ([[ultracode-audit-2026-07-09]]).

---

## 2026-07-09 — Dependency major-group migration: 19 of 20 majors (Dependabot #43 → PR #51)

- **Goal:** Clear the open Dependabot PRs. Merge the safe ones; take on the major-group PR #43 (20 updates) that can't even `npm ci` as-is.
- **Did (safe PRs, merged first):** #42 (dev minor/patch: @types/howler, autoprefixer — later removed by TW4), #38 (actions/setup-node 4→6), #40 (actions/checkout 4→7). All squash-merged, branches deleted, `main` pulled.
- **Did (PR #51, `95f6f9c`):** Branch `deps/major-group-migration`, squash-merged. Landed **19 of 20** majors from #43 in dependency-ordered clusters, each gated on lint + validator + `test:run` + build. **C1 runtime (`47a657c`):** react/react-dom 18→**19.2.7** (+ @types 19), framer-motion 11→**12**, zustand 4→**5**, immer 10→**11**, @testing-library/react 15→**16** (React 19 peer, pulled fwd). **C2 toolchain (`0c8ae85`):** vite 7→**8** (Rolldown bundler), vitest 3→**4**, vite-node 3→**6**, jsdom 24→**29**, @vitejs/plugin-react 4→**6**, fast-check 3→**4**. **C3 styling/lint (`b9a6633`):** tailwindcss 3→**4** (CSS-first — `@tailwind` directives → `@import "tailwindcss"`, `theme.extend` → `@theme` block in `src/index.css`, postcss plugin → `@tailwindcss/postcss`, dropped autoprefixer, deleted `tailwind.config.js`); eslint 9→**10** + @eslint/js 10 + eslint-plugin-react-hooks 5→**7** + globals 15→**17**. **Lockfile regen (`8c35b25`):** the CI-blocking fix.
- **Three code fixes (not just version bumps):** (1) `DeductionButton` selected `caseData?.recipes ?? []` — a fresh array each render; zustand v5's strict `Object.is` snapshot caching turned that into an infinite re-render loop under React 19 → stable `NO_RECIPES` module constant. (2) Vite 8/Rolldown dropped the **object** form of `build.rollupOptions.output.manualChunks` → rewrote the react/motion/audio vendor split as a function matching node_modules paths. (3) ESLint 10 + react-hooks 7 recommended presets newly enforce `set-state-in-effect`, `refs`, `no-useless-assignment` against working, intentional code → opted out in `eslint.config.js` (with rationale) to preserve the repo's deliberately-lean lint contract rather than fold a behavioural refactor into a deps bump.
- **The CI trap (worth remembering):** first CI run failed — `test` + `npm audit` died in ~10s on `npm ci --ignore-scripts` with "Missing @emnapi/core / @emnapi/runtime from lock file" (green locally on macOS). The Vite 8 (Rolldown) + Tailwind 4 (oxide) native pkgs pull `@emnapi/*` via their wasm32-wasi variants; an incremental `npm install` on macOS omitted them from the lockfile, but Linux `npm ci` demands them. Fix: `rm -rf node_modules package-lock.json && npm install`, verified with `npm ci --ignore-scripts` before re-push. Saved to auto-memory as [[dependabot-major-group-migration]].
- **Deliberately excluded — TypeScript 5→7 (1 of 20):** blocked upstream. `typescript-eslint@latest` **and** `@canary` both hard-cap `typescript` at `>=4.8.4 <6.1.0`; taking TS 7 breaks the lint gate entirely. Held TS at 5.x. **Closed #43** (superseded, with a comment listing what landed + why TS 7 was excluded); Dependabot should reopen a TS-only PR on its next run.
- **Decision:** Recorded in **ADR-0008** — clustered incremental migration over one big-bang PR; TS 7 deferral; lint-rule opt-out over refactor.
- **Verified:** On merged `main`: `npm run lint` clean, `node scripts/validateCase.mjs` → 7 clean, `npm run test:run` → **554 passed (554)** / 56 files (unchanged), `npm run build` green. **CI green on PR #51 — all six checks incl. the Cloudflare Workers Build (deploy preview).** Tailwind 4 render **verified in a real browser** (dev server + Playwright): title + character-creation screens correct — custom `gaslight-*` palette, fonts, borders, layout intact; 0 console errors. Confirmed built CSS emits all *used* custom + stock utilities.
- **Doc-drift sweep (auto-fixed):** "React 18" → "React 19" in `CLAUDE.md` (intro), `docs/README.md`, `docs/architecture.md` (+ "Tailwind CSS" → "Tailwind CSS v4" in the same three); "Vitest 3" → "Vitest 4" in `CLAUDE.md` Testing section. `docs/status.md` states no tool versions (content + test counts unchanged) — untouched. Audit/plan/ADR docs left as point-in-time records.
- **Open / blockers:** **TypeScript 7 remains pending** (external block on typescript-eslint). Otherwise all npm majors current. Audit backlog still clear except #20 (media, user-blocked). Watch: the `manualChunks` function + `@theme` tokens must survive future config edits; keep the lockfile's native-dep tree intact (regen fully, don't hand-edit) on future Vite/Tailwind bumps.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☑ (ADR-0008) · auto-memory ☑ ([[dependabot-major-group-migration]]).

---

## 2026-07-09 — Deployment migration: GitHub Pages → Cloudflare Worker (issue #47, PRs #48 + #49)

- **Goal:** Retire GitHub Pages and deploy the game as a Cloudflare static-assets Worker at `holodeck.jonwhitefang.uk/gaslight-and-grimoire/` (first entry in the site's new Holodeck section). In-repo config only; Cloudflare-side setup is owner-managed.
- **Did (PR #48, config — `2e539fa`):** Branch `p3/cloudflare-worker-deploy`, commit `cda8587`. Added `wrangler.jsonc` (assets-only Worker, no script, `assets.directory` → `./dist`); added `public/_headers` (ports the `<meta>` CSP to a real response header on `/*` **and adds `frame-ancestors 'none'`** — closes the F-037 residual); `deploy.yml` renamed to `CI` and stripped of the Pages publish (removed the `deploy` job, `upload-pages-artifact`, `pages`/`id-token` perms → `contents: read`, the `github-pages` environment, the `pages` concurrency group), keeping the lint + validator + Vitest gate + a build-compiles check. Kept the `index.html` `<meta>` CSP (authoritative CSP is now the header; `<meta>` covers local `vite dev`/`preview` + defence-in-depth) and refreshed its stale comment. **PR opened with `Refs #47` (no close keyword) per instruction to keep the issue open pending live confirmation.**
- **Did (PR #49, output nesting — `7144d34`):** Owner's live check found a 404: Vite `base` rewrites URLs but doesn't nest the physical `dist/` output, and the Worker maps request paths 1:1 onto files. Branch `p3/cloudflare-nest-output`, commit `2752378`. Added `scripts/nest-for-cloudflare.mjs` postbuild step (moves `dist/*` into `dist/gaslight-and-grimoire/` **except** `_headers`/`_redirects`, which Cloudflare only reads from the assets-root) and wired it into `build`. Confirmed runtime content fetches already prepend `BASE_URL` (`contentLoader.fetchJson`), so `/content/*` resolves under the nested path — no code change needed there.
- **Did (Pages teardown):** After owner re-verified all live checks green, disabled Pages via `gh api -X DELETE /repos/.../pages` (was `public:true`, `build_type:workflow`); `GET .../pages` now 404s. Old URL may serve stale 200 briefly from CDN edge cache — config is deleted, not re-published. **Closed issue #47** (`gh issue close --reason completed`).
- **Decision:** Recorded in **ADR-0007** (Enacted) — assets-only Worker over keeping Pages / changing `outDir` / a full Worker script; consequences incl. single deploy path (no Pages fallback), the non-obvious nesting build step, and accepted Bot Fight Mode console noise.
- **Verified:** `npm run test:run` → **554 passed (554)**, 56 files (a single flaky fail appeared in one resource-starved *background* run — 4000s duration — and did **not** reproduce on a clean re-run). `npm run build` green, emits `dist/gaslight-and-grimoire/` + `dist/_headers`. CI green on both PRs; **Cloudflare Workers Build check passed on PR #49** (git-connection confirmed live). **Owner-verified live:** `curl -sI .../gaslight-and-grimoire/` → 200 with CSP incl. `frame-ancestors 'none'`; `content/manifest.json` + a real case file 200; renders + character-creation works end-to-end in a real browser; root fallback 301 → `jonwhitefang.uk/holodeck`.
- **Doc-drift sweep (auto-fixed):** replaced every "deployed to GitHub Pages" / Pages-deploy claim with the Cloudflare Worker in `CLAUDE.md` (intro, dir-layout `deploy.yml` line, CI/CD section — rewrote to two jobs + a deployment bullet), `docs/architecture.md` (overview + base-path rationale), `docs/README.md` (intro), `docs/content-authoring.md` (content-layout base-path note). Audit/plan docs left as point-in-time records.
- **Open / blockers:** None for deploy — #47 done. **The entire audit backlog remains cleared except #20 (media — ambient loops + perceptual SFX QA, partly user-blocked).** Watch item: the nesting postbuild step must survive future `build`-script edits; keep the two CSP strings (`_headers` + `index.html` `<meta>`) in sync.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☑ (ADR-0007).

---

## 2026-07-08 — P3 #22 polish: performance + accessibility (PR #46)

- **Goal:** Close the last code-actionable audit issue — #22, the perf + a11y polish batch (8 findings, F-044..F-051). After this the whole Ultracode backlog is done except the user-blocked media milestone (#20).
- **Did:** Branch `p3/perf-a11y-polish`, single commit `1d120f5`, **PR #46 merged (`1ac3c09`)**. **Perf:** F-044 rAF-throttle the EvidenceBoard scroll/resize/mousemove handlers (coalesce to one recompute/getBoundingClientRect per frame, cancelAnimationFrame on cleanup); F-045 wrap all object-returning selector hooks in `useShallow`, memoize ChoicePanel's `revealedClueIds`/`deductionIds` Sets, `React.memo` ChoiceCard + ClueCard; F-046 adopt `LazyMotion` + `m` (domAnimation) across all 9 `motion.*` sites with the provider at the app root — **motion vendor chunk 121.85 KB → 79.13 KB** (gzip 40.3 → 28.1); F-047 module-cache the shared breakdown/incapacitation scenes and fetch them alongside the main `Promise.all` (was a 2nd sequential fetch per load) + `_resetSharedScenesCache` test hook. **A11y:** F-048 gate ConnectionThread/DeductionButton framer animations on `reducedMotion` (JS-driven, so the CSS reduced-motion rule can't reach them); F-049 typewriter-skip is now a real focusable `<button>`, visible text is `aria-hidden`, and a **state-driven** sr-only region announces the full narrative once on completion (no per-tick aria-live spam); F-050 informational helper text off `text-stone-500` (fails WCAG AA) → stone-400/300 (HC mode already whitens all stone shades); F-051 visually-hidden skip-to-content link + `id=main-content`/`tabIndex=-1` on the game `<main>`.
- **Review:** spike-first on F-046 (converted OutcomeBanner, ran its test in isolation to confirm `m` renders without a `LazyMotion` ancestor before doing the other 8). One correctness-finder subagent over the whole diff → **no hard bugs**; flagged one PLAUSIBLE a11y timing nit (SceneText reused across scene changes could briefly leak the new full text to the sr region before `animating` flipped, risking a double-announce) → fixed by making the sr region **state-driven** (`srText` set only at completion points, blanked at animation start) instead of reading `animating` at render.
- **Verified:** `npm run lint` clean; `npx tsc --noEmit` clean; `node scripts/validateCase.mjs` → 7 clean; `npm run test:run` → **554 passed (554)**, 56 files (was 547/55; +7: sharedSceneCache +3, SceneText +4). `npm run build` green, motion chunk shrunk. CI green on PR #46. **`Closes #22` auto-closed the issue on merge.**
- **Doc-drift sweep (auto-fixed):** test baseline **547→554 / 55→56** in `docs/status.md`; `injectSharedScenes` → `loadSharedScenes`/`mergeSharedScenes` rename reflected in `CLAUDE.md` (contentLoader line) and `docs/engine-reference.md` (with the F-047 cache note). Audit/plan docs left as point-in-time records.
- **Open / blockers:** **The entire audit backlog #1–#22 is closed except #20 (media assets)** — ambient loops + perceptual SFX QA, a user step (generate audio per the prompt kit). No code-actionable audit work remains.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no non-trivial architectural decision — polish executing the backlog).

---

## 2026-07-08 — P3 #21 hardening: save/UX safety + storage/CSP (PR #45) + engine-reference doc rewrite (PR #44)

- **Goal:** Continue the P3 batch. First cleared the doc item flagged for two checkpoints (engine-reference still described `narrativeEngine.ts` as one module — PR #44), then took the **#21 hardening** batch (7 findings) over #20 (user-blocked media) and #22 (polish), because #21 contains a real logic bug and data-loss risks.
- **Did (PR #44, docs):** Rewrote `docs/engine-reference.md` — `narrativeEngine.ts` documented as a barrel; its 4 split modules (`contentLoader`/`conditions`/`choiceResolution`/`encounters`) each get a `##` section; added `advantage`/`flags`/`constants`/`haltScenes` (18 non-barrel modules, one section each); fixed `last-critical-faculty` flag → typed field, `CURRENT_SAVE_VERSION` 2→3, missing `save()` `caseTitle?` param. Docs-only, CI green, merged `472ef32`.
- **Did (PR #45, #21):** Branch `p3/hardening-save-ux-safety`, single commit `15f6cc0`. **F-057** (logic bug) — `checkVignetteUnlocks` returned only the first satisfied vignette; now returns `string[]` (all), `completeCase` flags each; `CaseCompletionResult.vignetteUnlocked` → `vignettesUnlocked: string[]`; completion screen shows singular/plural. **F-036** — `SaveManager.load` added an envelope guard (reject non-object blob/state *before* `migrate`, protecting its `...state` spread) + `isValidGameState` shape guard after migration → corrupt save returns null. **F-052** — `saveGame` returns `{ evicted }`; App shows a nonce-keyed toast (success + eviction warning). **F-054** — two-tap delete confirm (arm → "Confirm?", onBlur disarm). **F-055** — loading screen during a save's async content fetch. **F-037** — documented CSP residuals (style-src unsafe-inline load-bearing for framer per-frame + dynamic `style={{}}`; no frame-ancestors via `<meta>` on Pages). **F-038** — `npm ci --ignore-scripts` in all 3 CI jobs (only esbuild has an install script, verified no-op for build/test/audit).
- **Review:** TDD for the two logic changes (F-057, F-036 — RED tests written first, watched fail, then GREEN). One correctness-finder subagent over the whole diff → **no bugs**; flagged one cosmetic toast-timer nit (identical repeat message didn't reset the 3s dismiss) → fixed with a monotonic nonce on the toast object.
- **Verified:** `npm run lint` clean; `npx tsc --noEmit` clean; `node scripts/validateCase.mjs` → 7 clean; `npm run test:run` → **547 passed (547)**, 55 files (was 524/53; +23: saveValidation, metaSlice.saveGame, expanded caseProgression/LoadGameScreen). `npm run build` green. CI green on PR #45 (test incl. `--ignore-scripts` install + lint, build, OWASP; deploy skipped on PR). **`Closes #21` auto-closed the issue on merge** — first use of the auto-close keyword after saving that to memory; no manual `gh issue close` needed.
- **Doc-drift sweep (auto-fixed):** test baseline **524→547 / 53→55** in `docs/status.md`; corrected two CLAUDE.md drifts in the touched area — `completeCase` "last-critical-faculty flag" → typed field + noted F-057 all-unlocks behavior, and added the F-036 `load` shape-guard bullet to the Save System section.
- **Open / blockers:** Only **P3 #22** (perf + a11y polish) remains code-actionable; **#20** (media — ambient loops + perceptual QA) stays partly user-blocked. `engine-reference.md` module-split item is now DONE.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no non-trivial architectural decision — hardening + docs executing the audit backlog).

---

## 2026-07-08 — Remaining P2 backlog (#15/#16/#17): tooling, perf, docs

- **Goal:** Close the last three open P2 audit issues — #15 (tooling/CI hygiene), #16 (perf), #17 (docs drift) — in one session. They touch disjoint concerns but overlap slightly on `App.tsx`, so worked as one branch/commit.
- **Did:** Branch `p2/tooling-perf-docs-cluster` off `main`, single commit `2171185` (not yet pushed). Verified each audit finding against *current* code first (audit was a day old; e.g. F-058's save-version claim had already been fixed to v3 by the #13-cluster, but its `saveManager` Date.now() claim was still wrong). **#15:** ESLint 9 flat config (`eslint.config.js` — TS recommended + `eslint-plugin-react-hooks`, test files exempt from `no-explicit-any`), `npm run lint` + lint step in the deploy `test` job; pinned Node (`.nvmrc` = 20.19 + `engines`, CI → `node-version-file`); removed unused `@testing-library/user-event`; dropped `npm audit` from the deploy `build` job (security.yml already runs it every PR — F-032); added `.github/dependabot.yml` (weekly grouped npm + actions). ESLint's first run caught real issues fixed here: a rules-of-hooks false-trip on a store action locally named `useAbility` (renamed → `activateAbility`), a `prefer-const` in encounters.ts, and 4 dead imports. **#16:** F-042 — new `useGameState` hook = `useStore(useShallow(buildGameState))`, replacing bare `useStore(buildGameState)` in `useCurrentScene`/ChoicePanel/HeaderBar (the fresh-object snapshot was a de-facto full-store subscription, re-rendering all consumers on any composure tick / flag set); +2 targeted tests. F-043 — `React.lazy` the 4 overlays + CaseCompletion behind `<Suspense>` (+ `OverlayFallback`), `manualChunks` splits react/framer-motion/howler; entry chunk **410 KB → 92 KB**. **#17:** fixed CLAUDE.md drift (removed `<GameScreen>` from the hierarchy, added `connections`/`addConnection`/`clearConnections` to the store table, corrected the `saveManager` Date.now/Math.random claim, refreshed the CI/CD section, added `npm run lint` to Commands); replaced the stale strikethrough "Known Bugs & Gaps" changelog with a lean pointer to the live docs; **added root `README.md`**.
- **Review:** dispatched one parallel correctness-finder subagent over the runtime changes (selector stability / missed re-renders, lazy+Suspense coverage incl. the two SettingsPanel sites, const-safety, vite config) → **no correctness bugs**; it flagged one cleanup (imports below the lazy consts after the edit) which was fixed pre-commit.
- **Verified:** `npm run lint` → clean; `npx tsc --noEmit` → clean; `node scripts/validateCase.mjs` → 7 cases clean; `npm run test:run` → **524 passed (524)**, 53 files (was 522/52; +2 `useGameState` suite); `npm run build` → green, chunks split (entry 92 KB + react 134 KB / motion 122 KB / audio 36 KB vendor chunks + lazy overlay chunks). CI not yet run (branch unpushed).
- **Doc-drift sweep (auto-fixed):** test baseline **522→524 / 52→53** in `docs/status.md`. **Flagged (not auto-fixed):** `docs/engine-reference.md` still documents `narrativeEngine.ts` as one module (carried over from last session) — a deeper doc restructure, out of scope for a checkpoint sweep and out of #17's scope (which was the F-058..F-062 findings, all addressed).
- **Open / blockers:** Push branch + open PR + merge + close #15/#16/#17. Remaining backlog: **P3 #21/#22** (code polish), **#20** (media — ambient loops + perceptual SFX QA, partly user-blocked). `engine-reference.md` module-split doc still pending a dedicated pass.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no non-trivial architectural decision — standard tooling/perf/docs fixes executing the audit backlog).

---

## 2026-07-08 — P2 refactor cluster (#13/#14/#18/#19): internal-quality hardening (PR #35)

- **Goal:** Knock out the largest coherent backlog batch in one PR. Chose the 4 refactor/type-safety issues (#13/#14/#18/#19, ~15 findings) over the tooling/perf/docs issues — they overlap on the same core files (`narrativeEngine`, `narrativeSlice`, `types`), so batching avoids serial rebase churn; all "no behavior change, tests green" except one intended UI fix.
- **Did:** Brainstorm→plan→subagent-driven execution. **Upfront: 2 parallel Explore agents** mapped exact current state (the issues' line numbers were stale post-#6) — this caught that **F-027 was already done** and **F-022 was half-done** (regular path already guarded; only the encounter twin needed it), so we didn't re-implement them. **Plan:** `docs/superpowers/plans/2026-07-08-internal-quality-hardening.md`, 11 tasks sequenced foundational→type-safety→riskiest-last. **Executed** each task as a fresh implementer subagent + 2-stage review (spec then quality), serialized (never parallel — shared files). Shipped as **PR #35** (merged `760f182`), 16 commits. New SoT modules: `flags.ts` (F-018), `constants.ts` (F-067), `advantage.ts` (F-014). `narrativeEngine.ts` (600+ lines) split into `contentLoader`/`conditions`/`choiceResolution`/`encounters` behind a **barrel re-export** → zero importer edits (F-019). `Condition` → discriminated union, casts dropped (F-026). `lastCriticalFaculty` → typed `Investigator` field, not a smuggled boolean flag (F-013). Save migration coerces missing/NaN version to 0 (F-015). Encounter non-check nav guard (F-022). `resetForNewCase`/`vignetteToCaseData` extracted (F-063/F-066). Dice `PRIMARY_FACULTY` derived from `ARCHETYPES` (F-065). worldSlice effect switch gets `assertNever` (F-064). **One intended behavior change:** Advantage badge now shows for a Lore check under Veil Sight (all 3 advantage sites unified through `computeAdvantage`; badge previously omitted the veil-sight rule).
- **Review:** every task spec+quality reviewed (2 review subagents each, verifying by reading code, not trusting reports); several rounds surfaced real Minor fixes applied as polish (flags.ts triplicate-literal de-dup, `ARCHETYPE_ABILITY_FLAG` typing, end-to-end badge-wiring test, unused imports). Task 5's test-count *drop* (removed 1 obsolete invalid-faculty test that a typed field makes unrepresentable) was scrutinized and confirmed legitimate — happy/unset paths retained. Task 9 (discriminated union, highest blast radius) reviewed with zero findings. **Final whole-branch integration review: READY TO MERGE**, no Critical/Important — verified flags/constants/advantage genuinely used everywhere, `lastCriticalFaculty` round-trips across all touching tasks, barrel acyclic, no test weakening.
- **Verified:** `npm run test:run` → **522 passed (522)**, 52 files (was 495/47; +27). `node scripts/validateCase.mjs` → 7 clean. `npm run build` + `npx tsc --noEmit` green (tsc-clean is the proof the discriminated union + barrel satisfy every consumer). CI green on PR #35 (test/build/OWASP; deploy skipped on PR, ran post-merge). Post-merge: closed #13/#14/#18/#19 by hand (commit/PR wording referenced them without GitHub auto-close keywords).
- **Doc-drift sweep (auto-fixed):** test baseline **495→522 / 47→52** in `CLAUDE.md`, `docs/status.md`, `PROJECT_STATE.md`. **Architecture drift:** updated `CLAUDE.md` engine directory-layout to list the 4 split modules + `flags.ts`/`constants.ts`/`advantage.ts` and note `narrativeEngine.ts` is now a barrel (behavior-section headers left — behavior unchanged, just relocated). **Flagged (not auto-fixed):** `docs/engine-reference.md` still documents `narrativeEngine.ts` as one module — a deeper restructure than a checkpoint sweep should do; noted for #17 (docs) or a dedicated pass. RUN_LOG/ADR left as point-in-time records.
- **Open / blockers:** None. Backlog: **P2 #15/#16/#17** (unblocked, viable now), **P3 #20 (media — ambient loops + QA, partly user-blocked)/#21/#22**. Next code work starts from a fresh branch off `main`.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new architectural decision — refactor executing already-decided invariants from ADR-0001; the plan doc is the design record).

## 2026-07-08 — Media assets (#20): 9 SFX landed + normalized; in-browser QA caught & fixed 2 release-blockers

- **Goal:** User added the 9 SFX files; check them, then QA the audio in Playwright.
- **Did (assets):** Verified all 9 SFX present, correctly named (incl. `clue-red-herring.mp3`), valid MP3s, no dupes (unique MD5s). Installed **ffmpeg** and audited: loudness spread was a wild **34 dB (-10 to -44 LUFS)** with `clue-deduction` clipping (+0.5 dBTP) and 6s long (only 3.24s audible). **Normalized all 9 in-place** (measure integrated LUFS → static gain to -14 → limiter → trailing-silence trim; two-pass loudnorm kept corrupting its own filter args, and static gain is better for short one-shots anyway). Result: **~6 dB spread around -14 LUFS**, clip gone, padding trimmed. Originals backed up to `/tmp/sfx-originals-backup/`. Accepted `clue-deduction` at 2.62s (content length, not padding — only re-gen shortens it).
- **Did (QA → 2 blockers found & fixed, both TDD):** Drove the running app via Playwright MCP. **Blocker 1:** selecting any main case threw a fatal content-validation error ("unknown key deduction") and bounced to case select — `validateContent` (narrativeEngine.ts:110) built its `validateBundle` **without `caseData.recipes`**, so the `requiresDeduction`/`hasDeduction` registry was empty. A **#6 regression** the CLI validator couldn't catch (it passes recipes) and #6's module-level E2E never exercised the live `loadCase→validateContent` path. Fix: pass `recipes: caseData.recipes` (1 line) + regression test in `validateContent.test.ts`. **Blocker 2:** SFX 404'd — `audioManager` hardcoded bare `/audio/sfx/*.mp3`, ignoring Vite's `/gaslight-and-grimoire/` base (broken in dev **and** on GitHub Pages; latent until files existed). `AmbientAudio` already prefixed `import.meta.env.BASE_URL`; SFX didn't. Fix: extracted pure `buildSfxSrc(base, event)` (new `audioManager.test.ts`, 3 cases), `getSfxHowl` resolves via `BASE_URL`; refactored `SFX_PATHS`→`SFX_FILES` (stems). Used the repo's stub-then-implement pattern so RED tests compiled past the `tsc` PostToolUse hook.
- **Verified:** `npm run test:run` → **495 passed (495)**, 47 files (was 491/46; +4). `node scripts/validateCase.mjs` → 7 clean. `npm run build` green. **In-browser (Playwright):** post-fix Whitechapel loads with **zero console errors** (was fatal throw); SFX fetch base-prefixed **200s** — confirmed live for `scene-transition` (scene entry), `clue-physical` (clue discovery), `dice-roll` (Reason check). **Not verifiable by machine:** perceptual SFX quality (needs human ears) — flagged as a user step.
- **Doc-drift sweep (auto-fixed):** test baseline **491→495 / 46→47** in `CLAUDE.md`, `docs/status.md`, `PROJECT_STATE.md`. Rewrote `status.md` Assets section (9 SFX now ship + normalized + base-path-aware; ambient still pending; corrected the "search returns nothing" line). No broken links. RUN_LOG/ADR left as point-in-time records.
- **Open / blockers:** Media milestone continues — **10 ambient loops** + **perceptual SFX QA** (human) pending, then `checkAudioAssets.mjs` + CI. 2 fix commits on `fix/audio-and-deduction-load-blockers` (→ PR + merge this session).
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new decision — bug fixes, not architecture choices).

## 2026-07-08 — Media assets (#20): strategy decision + audio prompt kit (docs only)

- **Goal:** Brainstorm the media-assets milestone (#20) to a decision and produce whatever's unblocked. Resolve the standing "how do we source/license media?" open question.
- **Did:** Ran the brainstorming skill (with the visual companion, later stopped — no visual questions remained once illustrations went out of scope). **Decisions:** source = **AI-generated**; scope = **audio only** (illustrations **parked, lowest priority** — no code change to defer, `SceneIllustration` renders nothing when `illustration` is undefined); **9 SFX first, then 10 ambient loops**; pipeline = **prompt kit the user runs** (not an API integration — assistant can't synth audio, and this keeps keys/billing out of the repo); house style = **naturalistic Victorian period ambience, never campy** (supernatural leaks only at the occult stinger + `cellar`/`seance` beds); `checkAudioAssets.mjs` validator + CI wiring **deferred until files land**. Wrote the design spec (`docs/superpowers/specs/2026-07-08-audio-asset-kit-design.md`) and authored the prompt kit (`docs/audio-asset-kit.md`) — all 19 per-asset prompts, tool recs (ElevenLabs SFX / Stable Audio·Suno), format+loudness targets (~-14 LUFS SFX / ~-18 LUFS ambient), post-processing + manual-QA checklists, regeneration guide. Added `.superpowers/` to `.gitignore`. **3 commits** on `main` (`f6bc29e` spec, `42de32f` decisions, `19908f9` kit) — **unpushed**.
- **Verified:** Docs-only — **no code, content, or test change**; baseline stays **491/46** (not re-run; nothing executable changed). Prompt-kit asset names verified 1:1 by grep: the 9 filenames match `SFX_PATHS` in `audioManager.ts` (incl. the `clue-redHerring` event → `clue-red-herring.mp3` file asymmetry, documented in the kit), and the 10 `ambient-*.mp3` names match the distinct `ambientAudio` values across `public/content/**` — exact match, no typos that would cause silent-failure.
- **Doc-drift sweep:** Narrow (docs-only session). Resolved the media-sourcing **open question** in PROJECT_STATE → pointer to new **ADR-0006**; flipped the Media milestone row `[ ]`→`[~]`; rewrote header, Current-position (branch = 3 unpushed doc commits; Verification = docs-only, baseline unchanged), and Next-actions (4-step asset-blocked sequence). Wired ADR-0006 into DECISIONS/README + References. No broken internal links; no stale version/status facts found elsewhere (test baseline untouched, so no count drift this session).
- **Open / blockers:** **Asset-blocked** — steps 2–4 (build `checkAudioAssets.mjs` + test, QA pass, CI wiring) wait on the user generating the 19 files. Step 1 (generation) is the user's unblocked next move. 3 commits unpushed to origin. Illustration milestone parked (lowest priority).
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☑ (ADR-0006 — media asset strategy).

## 2026-07-08 — #6 deduction-gated content: stable deduction identity + true endings (last P1)

- **Goal:** Close **#6**, the last open P1 — make the deduction mechanic load-bearing. Brainstormed the design (recorded decisions: named clue-set recipes; gate the *true* resolution only; one key deduction per main case; subset match), wrote spec + plan, then executed via subagent-driven development (implementer + spec-review + quality-review per task; content tasks also through `content-integrity-reviewer`).
- **Did:** Shipped as **PR #32** (merged `b319f09`), 16 commits. **Mechanic:** new `KeyDeduction` type + optional `CaseData.recipes` (`src/types/index.ts`); pure `matchDeduction` (subset) + `buildDeductionFromRecipe` (`buildDeduction.ts`, TDD); `loadCase` fetches optional `deductions.json` → `caseData.recipes` (outside `Promise.all`, `.catch(()=>[])`); `DeductionButton` stores a matched deduction under the recipe's **authored id** so `hasDeduction`/`requiresDeduction` gates resolve (random-id fallback preserved); validator + CLI enforce recipe clue refs and gate targets against the recipe-id registry (`hasDeduction`/`hasFlag` case split — flags stay free-form). **Content:** one recipe + one `requiresDeduction`-gated reckoning choice + one new true-ending scene in each main case — Whitechapel (`wc-deduction-harland-mastermind` → Harland exposed), Mayfair (`ms-deduction-fraud-and-breach` → dual-truth account), Lamplighter's (`lw-deduction-croke-court-murder` → Croke/Court, also gated on `lw-bell-alliance`). Existing endings remain reachable (failure/fumble route to a pre-existing ending; case always completable).
- **Content-integrity-reviewer catches (all fixed):** Whitechapel called Harland "Superintendent" with an invented superior, contradicting the clue that names him Deputy Commissioner → aligned to Deputy Commissioner across all three references; Mayfair "the sigil they copied" implied the fraudsters did the occult work (contradicts Vesper-as-genuine-practitioner) → softened to "copied into their stage-dressing", plus re-keyed a redundant in-recipe `advantageIf` to `ms-clue-guest-testimony` and fixed `séance` spelling; Lamplighter's true ending ignored the live Veil-Key breach its parent scene opens on → added breach-resolution sentence + `lw-key-in-yard-custody` flag, and gated the choice on `lw-bell-alliance` (naming Croke to Bell requires Bell present).
- **Verified:** `npm run test:run` → **491 passed (491)**, 46 files (was 481/44; +10: matchDeduction 6, contentValidation.deduction 4; 0 regressions). `node scripts/validateCase.mjs` → 7 cases clean. `npm run build` (`tsc && vite build`) green. **In-browser E2E via Playwright MCP** against the real app modules (`loadCase`/`matchDeduction`/`buildDeductionFromRecipe`/`isChoiceVisible`): recipe reaches `caseData`; connecting recipe clues (+noise) stores under the authored id; partial (2/3) connection → no match; gated choice hidden without the deduction, visible with it, routing to the true ending. Final cross-task integration review: approved, no Critical/Important. CI green on PR #32 (test✅/build✅/OWASP✅, deploy skipped on PR); #6 auto-closed.
- **Process note:** two implementer subagents were briefly run in parallel on the same branch (T5/T6) and hit a commit race (index.lock); no work lost, but I resolved it by hand and thereafter **serialized all implementers** — the skill's "no parallel implementers" rule earned. Also gitignored `.playwright-mcp/` session artifacts.
- **Doc-drift sweep (auto-fixed this session's drift):** test baseline **481→491 / 44→46 files** in `CLAUDE.md`, `docs/status.md`, `PROJECT_STATE.md`. Documented `deductions.json`/`KeyDeduction` (new "Key deductions" section + content-layout/file-shape rows + `hasDeduction` catalog row now pointing at the recipe registry) in `content-authoring.md`; added `matchDeduction`/`buildDeductionFromRecipe` and the `loadCase` recipes fetch + validator recipe rules to `engine-reference.md`; CLAUDE.md directory-layout + `buildDeduction.ts` module note + a new content-authoring rule for key deductions. No broken internal links. RUN_LOG/audit left as point-in-time records.
- **Open / blockers:** **P1 backlog fully cleared.** Sole remaining pre-1.0 milestone is **media assets (#20 + #21/#22)** — blocked on the sourcing/licensing decision (open question in PROJECT_STATE). P2/P3 polish (#13–#22) remain as lower-priority backlog. No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☑ (ADR-0005 — stable deduction identity via key-deduction recipes).

## 2026-07-07 — P1 code cluster (#7 touch-connect, #8 a11y, #9 halt screen, #10 titles, #12 tests) + 2 review fixes

- **Goal:** Knock out the P1 audit issues that are well-specified code fixes (deferring #6, which is creative content). User chose: code cluster #7/#8/#9/#10 + #12 tests; #9 as a distinct "Investigation halted" screen (not a recovery choice). Branch `fix/p1-code-cluster-touch-a11y-halt-titles`. TDD throughout (RED watched before each fix).
- **Did:** **#10** — new `src/utils/caseTitle.ts` (`deslugifyCaseId`, `resolveCaseTitle`); HeaderBar renders `caseData.meta.title` (slug fallback); `SaveManager.save` takes an optional `caseTitle` → save index stores the readable title; `saveGame`/`autoSave` pass it. **#7** — `ClueCard` now connects on tap/click (`onClick`) and Enter (added to keydown), misleading `draggable`/`cursor-grab` removed (→ `cursor-pointer`); EvidenceBoard shows a persistent first-time hint and tap-worded in-progress hint. **#8** — new shared `src/hooks/useFocusTrap.ts` (capture→initial focus→Tab trap→restore), applied to EvidenceBoard/CaseJournal/NPCGallery; background `inert` while any overlay is open (`App.tsx`); real high-contrast stylesheet in `index.css` (surface/text/accent/border overrides under `.high-contrast`). **#9** — new pure `src/engine/haltScenes.ts` (`isHaltScene`/`haltReason`, keyed on `variantOf ?? id` so case variants like `wc-breakdown` are caught) + new `InvestigationHalted` component; `GameContent` renders it (precedence over the `isTerminal` "Case Complete" branch), `onHalt` returns to case selection. **#12** — App routing tests (incl. load-failure→title), `goToScene` onEnter-once (F-006 guard), dice band/trained-bonus tables (DC-3 partial boundary), typed the integration mock as `EngineActions` (dropped `as any`), `validateContent` wrapper, ErrorBoundary, connections/visitedScenes round-trip, chained v0→v3 migration.
- **Adversarial review (5-dimension workflow, one skeptic per finding):** 5 review agents (2 died on API errors after their surfaces were already hand-audited); **2 findings confirmed (refuted=false) and fixed** — **HIGH:** `loadAndStartCase`/`Vignette` never reset composure/vitality or cleared `breakdown-occurred`/`incapacitated`, so a knockout re-fired breakdown on every subsequent case (and poisoned the scene-frequency autosave) — a guaranteed loop my #9 routing made reachable → both loaders now restore meters to 10 and clear the halt flags on load (TDD in `narrativeSlice.test.ts`). **LOW:** `LoadGameScreen` rendered `save.caseName` verbatim, so legacy (pre-change) saves showed the slug → added a render-site `displayCaseName` that de-slugifies slug-shaped names.
- **Verified:** `npm run test:run` → **481 passed (481)**, 44 files (was 384/30; +97 tests, 0 regressions). `node scripts/validateCase.mjs` → 7 cases clean. `npm run build` (`tsc && vite build`) green. High-contrast verified visually via Playwright MCP (before/after screenshots: dark-amber theme → black bg, yellow accents, white text, yellow primary button). PostToolUse type-check hook stayed green throughout (used the stub-then-implement pattern for new modules so RED tests still compile).
- **Doc-drift sweep (auto-fixed this session's drift):** test baseline **384→481 / 30→44 files** in `docs/status.md`, `CLAUDE.md`, `docs/PROJECT_STATE.md`. `status.md` — rewrote the Accessibility bullet (high-contrast is now a real theme + `useFocusTrap` + `inert`, not merely "applied via AccessibilityProvider") and the Recovery/halt bullet (distinct halt screen + meter reset on case load). Removed a duplicated media-assets line in `PROJECT_STATE.md` Next-actions. No broken internal links introduced. RUN_LOG/audit left as point-in-time records.
- **Open / blockers:** **#6 (deduction-gated content)** is the last open P1 — deferred as creative narrative work, now unblocked by #7. Then media assets (#20). No blockers. Shipped as **PR #30** (merged `386699a`; CI test✅/build✅/OWASP✅, deploy skipped on PR; issues #7–#10/#12 auto-closed).
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new architectural decision — all fixes sit within existing patterns; `useFocusTrap`/`haltScenes` are implementations, not new directions).

## 2026-07-07 — Clear final P0 group: encounter escape (#4) + onEnter idempotency (#5), +F-022/F-027

- **Goal:** Finish the audit's P0 backlog — the two remaining engine bugs (#4, #5) — and fold in any adjacent findings that live on the same engine surface and make sense to fix in the same pass.
- **Did:** One branch/PR (**PR #28**, merged `2efb40a`), TDD throughout (RED watched before every fix). **#4 (F-004)** — `processEncounterChoice` now treats `isEscapePath` choices as terminal: navigates to the escape outcome and marks the encounter `isComplete` immediately, before the round-advance logic, applying no damage. **#5 (F-006/F-008)** — moved `onEnter` effect application out of the `NarrativePanel` useEffect into `goToScene`, gated on a new `narrativeSlice.visitedScenes` set so a scene's effects fire exactly once per playthrough; feedback text now flows through `narrativeSlice.lastEffectMessages` (panel only reads it). `visitedScenes` added to `GameState`/snapshot/`loadGame`; save migration **v2→v3** backfills it from `sceneHistory + currentScene`, and `CURRENT_SAVE_VERSION` bumped 2→3. Reset on case/vignette load. **Graft-ins on the same surface:** **F-022** — `computeChoiceResult` throws a clear error for a non-check choice with no `success`/`critical` outcome (was silent `goToScene(undefined)` → blank scene), plus a matching content-validator rule (partial F-016). **F-027** — removed the dead `_hasAdvantage` annotation from `getEncounterChoices` (no production consumer; `getEncounterChoices` now returns `Choice[]`); rewrote its 3 tests to assert the real advantage passed to `performCheck` inside `processEncounterChoice`. **Left out** (own PRs): #10 slug titles, F-013 typed `lastCriticalFaculty`, #9 breakdown dead-end.
- **Correction to the audit framing:** F-006's re-fire was attributed partly to the back button, but that button uses a read-only review overlay (`App.tsx` `setReviewSceneId`), not `goToScene` — so it never re-fired. The real trigger was **save-load** landing on an already-entered scene; the visited-set fix covers it. Also confirmed every recovery scene with a positive `onEnter` delta is a one-time story beat (no scene is designed to re-heal on re-entry), so once-per-playthrough is the correct semantics.
- **Verified:** `npm run test:run` → **384 passed (384)**, 30 files (was 368; +16 new tests, 0 regressions). `node scripts/validateCase.mjs` → 7 cases clean (new non-check-outcome rule flags nothing in existing content). `tsc --noEmit` + `npm run build` green. CI green on PR #28: test ✅ → build ✅, deploy skipped on PR, OWASP + npm audit ✅. Issues #4/#5 auto-closed → **0 P0 issues open**.
- **Doc-drift sweep (auto-fixed this session's drift):** `engine-reference.md` — rewrote `computeChoiceResult` (throws on no-outcome), `processEncounterChoice` (escape terminal), `getEncounterChoices` (now `Choice[]`, `_hasAdvantage` gone), `SaveManager.migrate` (added v2→v3, current version 3). `architecture.md` — data-flow diagram (onEnter now from `goToScene`, gated), narrativeSlice state row (+`visitedScenes`/`lastEffectMessages`). `CLAUDE.md` — narrativeSlice row, `applyEffects`/`processChoice` behaviour lines, encounter behaviour lines, Save System migration line (also fixed the **pre-existing** "Current version: 1" drift flagged as #17, since this session touched the migration), test baseline 368→384, and the `applyEffects`-from-view architectural warning (now onEnter-from-`goToScene`). `status.md` + `PROJECT_STATE.md` test baseline 368→384. No broken internal links introduced.
- **Open / blockers:** P0 backlog fully cleared. Next work queue is **P1** (issues #6–#12), starting with #6 (deduction-gated content). No blockers. Note: `CLAUDE.md:51` `<GameScreen>` and the high-contrast-inert items from #17/#8 remain open in their own issues (not touched here).
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new architectural decision — fixes within existing patterns; the `visitedScenes` gate implements the content↔engine/idempotency direction already established, ADR-0001).

## 2026-07-07 — Content-authoring automation layer + CLAUDE.md currency + Debt of Smoke design fixes

- **Goal:** Audit/improve `CLAUDE.md`, then design and wire in a set of Claude Code automations for the content-authoring workflow, and apply the design fixes the new reviewer surfaced.
- **Did:** **(1) CLAUDE.md currency** — corrected the stale CI/CD section (now describes the `test`→`build`→`deploy` job graph, `pull_request` trigger, PR-deploy skip), dropped the drifting "as of 2026-02-25" Known-Bugs date and pointed it at `status.md`/`PROJECT_STATE.md`, and added the two missing files (`engineActions.ts`, `scripts/validateCase.ts`) to the Directory Layout. **(2) Automation layer (ADR-0004)** — `PostToolUse` hook `.claude/hooks/post-edit-checks.sh` (validator on `public/content/**.json` edits + `tsc --noEmit` on `.ts`/`.tsx` edits, surfaces failures via exit 2, `TYPECHECK=0` escape hatch) wired into `.claude/settings.json`; read-only `content-integrity-reviewer` subagent (`.claude/agents/`) for the design/tone rules the validator can't check; new `/review-content` command; `/new-scene` scaffold skill + annotated `scene-template.json`. Wired the reviewer into `/new-scene` (final step) and `/checkpoint` (content gate, self-skips when no `public/content/` changed) since hooks can't dispatch subagents. Added `context7` + `github` MCP servers to `.mcp.json` (project scope; `gh auth login` done). **(3) Debt of Smoke fixes** — the reviewer's first run flagged 4 findings; fixed the 2 warnings: `dos-choice-who-threat` had an inert success band (success/partial routed to the same guarded scene as fumble) → now matches its influence sibling; the guarded branch routed straight to Act 2, skipping the only source of the central `dos-clue-ember-artifact` clue and Sable's payoff → rerouted through `dos-act1-sable-reveals`, with the guarded scene's closing line reworded so the relent reads coherently. Left the 2 nits (toothless red herring, twin Ashwick faculty paths) as optional.
- **Verified:** `content-integrity-reviewer` dispatched twice against Debt of Smoke — first run 4 findings, post-fix re-review **0 blockers / 0 warnings**, no regressions/orphans. `node scripts/validateCase.mjs` → 7 cases clean. `npm run test:run` → **368 passed (368)**, 30 files. `tsc --noEmit` clean (`src` scope). Hook exercised on 5 payloads: passing content JSON / passing TS / non-matching file all exit 0 quiet; **broken content edge → exit 2 with the validator error surfaced; broken TS → exit 2** — test fixtures restored.
- **Doc-drift sweep (auto-fixed this session's drift):** `PROJECT_STATE.md` said "up to date with origin at `dd18933`" and "post-merge deploy on `dd18933` running" — stale (HEAD is now `4036be6`, PR #26 merged, that deploy long done); corrected the branch-focus + verification lines to `4036be6`. `status.md` test baseline (368/30) and content counts still accurate — no change. No broken internal links introduced.
- **Open / blockers:** P0 engine bugs **#4** + **#5** still the next feature work (unchanged). Optional: 2 nit-level Debt of Smoke polish items; the `PostToolUse` type-check may prove noisy on multi-file refactors (mitigated by `TYPECHECK=0`). No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☑ (ADR-0004 — content-authoring automation layer)

## 2026-07-07 — Audit remediation: P0 group 1 (CI gate) + group 2 (validators + Debt of Smoke)

- **Goal:** Start clearing the audit's P0 backlog. Grouped the issues and knocked out two clusters: the CI safety net (#1 + #11) and the content-validator unification + the unreachable vignette (#2 + #3).
- **Did:** **PR #24 (merged `39a57f6`)** — added a `test` job to `deploy.yml` running the validator + `npm run test:run`; `build` → `deploy` now depend on it; added a `pull_request` trigger with `deploy` skipped on PR events (closes **#1**). Deploy quick-wins (closes **#11**): themed `public/favicon.svg` referenced via `%BASE_URL%` (was a `/vite.svg` 404); `cancel-in-progress: false`; pinned `dependency-check/Dependency-Check_Action` to SHA `75ba02d` (v1.1.0). **PR #25 (merged `dd18933`)** — new pure `src/engine/contentValidation.ts` (`validateBundle`) is now the single source of truth for both the runtime `validateContent` (delegates, errors only) and the CLI (`scripts/validateCase.ts` via `vite-node`, with `validateCase.mjs` kept as a launcher shim); extended checks to condition targets, variant structure, `npcEffect` refs, encounter-round recursion, `dynamicDifficulty` tier completeness, `clue.sceneSource`, + CLI reachability (closes **#2**). Fixed **F-024** engine bug (`evaluateCondition` `hasFlag` `{value:false}` never matched an unset flag → 6 recovery variants dead; now `Boolean(flag)===value`) and **F-023** (4 dangling Mayfair `sceneSource`s repointed). Re-gated *Debt of Smoke* from the unattainable `npc-sable` disposition ≥ 7 onto the persisted `wc-court-deal-made` flag (closes **#3**). Promoted `vite-node` to a declared devDependency. Built with TDD (RED watched before each fix).
- **Verified:** `npm run test:run` → **368 passed (368)**, 30 files (was 334/29). `node scripts/validateCase.mjs` → 7 cases clean; a deliberately-broken scratch case errors on every new check class and exits 1. `npm run build` + `tsc --noEmit` green. CI green on both PRs in the clean runner (test ✅ / build ✅ / deploy skipped on PR); OWASP scan ✅ with the pinned SHA. Issues #1/#11/#2/#3 auto-closed; 18 open remain.
- **Doc-drift sweep (auto-fixed this session's drift):** test baseline **334→368 / 29→30 files** in `docs/status.md`, `CLAUDE.md`, `PROJECT_STATE.md`. *Debt of Smoke* unlock (disposition ≥ 7 → `wc-court-deal-made` flag) in `CLAUDE.md` (×2), `docs/engine-reference.md`. Validator scope/CI-step description rewritten in `docs/content-authoring.md` + `docs/status.md` (now the shared module, wider checks, `test`-job step). Added `contentValidation.ts` to the module lists in `CLAUDE.md` + `docs/engine-reference.md`; corrected the `hasFlag`/`validateContent`/`checkVignetteUnlocks` descriptions there. RUN_LOG/audit records left as point-in-time (not rewritten).
- **Open / blockers:** P0 group remaining: engine bugs **#4** (encounter escape is a dead button before the final round) + **#5** (`onEnter` re-fires on revisit/load) — next session. No blockers. Post-merge deploy on `dd18933` running.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new architectural decision — fixes within existing patterns; shared-validator extraction is an implementation of the existing content↔engine separation, ADR-0001)

## 2026-07-07 — Full Ultracode repo audit + file findings as GitHub issues

- **Goal:** Run a comprehensive read-only, all-angles audit of the repo and produce a report for later remediation; then file the findings as GitHub issues.
- **Did:** Ran a 13-dimension adversarial audit (110 subagents: one finder per dimension, one independent skeptic re-opening cited files per finding). 97 findings verified, 5 rejected as false positives; deduped to **67 canonical findings** (2 High, ~19 Medium, ~40 Low/Polish). Wrote the report (now at `docs/audits/ULTRACODE_FULL_REPO_ANALYSIS.md` — the user moved it there from repo root). Created priority + category labels and filed **22 grouped GitHub issues** (#1–#22: 5 P0, 7 P1, 7 P2, 3 P3) on `JonWhiteFang/gaslight-and-grimoire`. Independently re-verified the anchor findings (e.g. `npc-sable` max disposition +4 vs unlock threshold 7 → *Debt of Smoke* unreachable; 0 `requiresDeduction`/`hasDeduction` refs in content; `ClueCard` has `draggable` + `onKeyDown` only, no click/touch). Updated PROJECT_STATE to correct the "media is the only remaining work" framing (now false), add an audit + a P0/P1 remediation milestone row, reorder Next actions, and link the report. **No code changed.**
- **Verified:** `npm run test:run` → **334 passed (334)**, 29 files (unchanged). `node scripts/validateCase.mjs` → 7 cases clean. `npm run build` green (402 KB JS / 125 KB gzip). `npm audit` → 0 vulns. `gh issue list` → 22 open issues. Audit method is read-only; runtime-behaviour findings (encounter escape, onEnter re-fire, touch connect) are confirmed from code+content and flagged in the report for an optional Playwright MCP click-through.
- **Doc-drift sweep (flagged, NOT auto-fixed — each is now a filed issue, so fixing piecemeal here would make the issue stale on day one):** (1) `CLAUDE.md:197` says save "Current version: 1" but `saveManager.ts:14` is `CURRENT_SAVE_VERSION = 2`; `CLAUDE.md:305` wrongly lists `saveManager` under `Date.now()/Math.random()` users → **issue #17**. (2) `docs/status.md:70`/`CLAUDE.md` present high-contrast as "applied", but the audit found the `.high-contrast` class is consumed by nothing (inert) → **issue #8**. (3) `CLAUDE.md:51` references a `<GameScreen>` component that doesn't exist → **issue #17**. Left these to their issues rather than editing four bundled drifts one at a time. No drift found in the spine itself.
- **Open / blockers:** 22-issue backlog is the new work queue; P0 issues (#1–#5) are correctness/CI blockers that should precede media assets. No blockers. The relocated report under `docs/audits/` is untracked — surfaced for the user to commit.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☐ (no new architectural decision — the audit records findings, it doesn't decide a direction)

## 2026-07-07 — Add Playwright MCP server (project scope)

- **Goal:** Install a browser-driving MCP capability so the running game can be verified end-to-end (the one open milestone, media assets, only shows up at runtime).
- **Did:** Added `@playwright/mcp` at **project scope** via committed `.mcp.json` (`claude mcp add playwright -s project`). Chose project over local/user scope for team-shareability, consistent with the repo's committed-tooling convention. Recorded the rationale in **ADR-0003** (Enacted) and wired it into the ADR index. Updated PROJECT_STATE: `_Last updated:_`, References (ADR-0003), and Next actions (media-asset steps now note Playwright can verify in-browser).
- **Verified:** `.mcp.json` valid JSON, registers `playwright`; `npx @playwright/mcp@latest --version` → 0.0.77; Playwright Chromium binary already cached. `claude mcp list` → server registered, `⏸ Pending approval` (expected one-time gate for committed MCP). Ground-truth baseline unchanged: `npm run test:run` → **334 passed (334)**, 29 files; `node scripts/validateCase.mjs` → 7 cases clean. Doc-drift sweep: no doc references MCP/Playwright, so no drift; STATE counts still accurate.
- **Open / blockers:** Playwright MCP needs one-time approval on next `claude` launch in this repo before its tools go live. Media assets remain the one open milestone. No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☑ (ADR-0003)

## 2026-07-07 — First `/checkpoint`: exercise the loop, enact ADR-0002

- **Goal:** Run the first real end-of-session `/checkpoint` to verify the memory-spine loop works, and enact ADR-0002 now that its follow-up condition is met.
- **Did:** Doc-drift sweep (scoped to this session's docs) found no drift — STATE/status.md `334/334` + `29 files`, content counts, and the CLAUDE.md hook path all match code/git/config. Flipped **ADR-0002 Accepted→Enacted** (status line annotated with the transition; Follow-up struck through with a dated note — original decision text preserved). Updated the ADR index row to Enacted. Bumped PROJECT_STATE `_Last updated:_` and marked both ADRs Enacted in References. Confirmed the `/checkpoint` skill and `SessionStart` hook load (post `/reload-plugins`: "2 hooks, 1 skill").
- **Verified:** `npm run test:run` → **334 passed (334)**, 29 files. `node scripts/validateCase.mjs` → all 7 cases clean. Working tree was clean at session start (prior spine work already committed as `04e7485` and pushed).
- **Open / blockers:** Media assets remain the one open milestone — see PROJECT_STATE Open questions. No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☑ (ADR-0002 → Enacted)

## 2026-07-07 — Stand up the committed memory spine

- **Goal:** Set up the committed, version-controlled project-memory spine (PROJECT_STATE / RUN_LOG / DECISIONS + session-start hook + `/checkpoint` skill), adapted to G&G's real state.
- **Did:** Added `docs/PROJECT_STATE.md` (one-page snapshot; phase tracker A–E done, media-assets the one open milestone; scope-gate tracker and scope ledger dropped as not applicable to a feature-complete project). Added `docs/RUN_LOG.md` (this file). Added `docs/DECISIONS/` with `ADR-TEMPLATE.md`, `README.md` (index), `ADR-0001-content-engine-separation.md` (architecture baseline, Enacted), and `ADR-0002-committed-memory-spine.md` (this system, Accepted). Added `.claude/hooks/session-preflight.sh` (git state + top of PROJECT_STATE) and wired a `SessionStart` hook into `.claude/settings.json` while preserving the existing `permissions` block. Added `.claude/skills/checkpoint/SKILL.md`. Added a "Project memory" pointer section to `CLAUDE.md`.
- **Verified:** `npm run test:run` → **334 passed (334)**, 29 files. `node scripts/validateCase.mjs` → all 7 cases clean. `.claude/settings.json` parses as valid JSON (`jq`). Hook script runs standalone and emits valid `additionalContext` JSON.
- **Open / blockers:** Media assets (audio + illustrations + NPC portraits) remain unbuilt — see PROJECT_STATE Open questions. No blockers.
- **Memory updated:** STATE ☑ · RUN_LOG ☑ · ADR ☑ (ADR-0001, ADR-0002)
